# このリポジトリを触るときのルール

CHANNEL DESK（【LightTHREE】 -ポーカーメディア‐の分析ダッシュボード）のデータと画面です。
**どのパソコン・どのアカウントで作業を始めても、まずこのファイルを読んでください。**

- 公開URL: https://ytpokerguild-netizen.github.io/channel-desk-data/index.html
- 構成: GitHub Pages（静的）＋ GitHub Actions（3時間おきのバッチ）。サーバーはありません
- `index.html` は約152KBの1ファイル完結（HTML+CSS+JS）。外部ファイルに切り出さないこと

---

## 1. 引き継ぎ書類の正本は Dropbox にあります

**このリポジトリには手順の詳細を置きません。リポジトリは public なので、
運営者の判断で「書類は公開される場所に置かない」方針です。**（2026-08-10 決定）

| ファイル | 中身 |
| :--- | :--- |
| `PGコンソール/引き継ぎ書_CHANNEL_DESK.md` | サイト全体の引き継ぎ。ファイル構成・秘密情報の扱い・過去の事故・保留中の宿題・次にやること |
| `PGコンソール/チャンネル分析AI_運用手順.md` | 週次AI分析の手順。分析者としての大原則、数字の落とし穴、出力フォーマット |

`PGコンソール` は Dropbox のフォルダで、セッションに接続されていれば
`mcp__remote-devices__*` のファイル操作ツールで読み書きできます。
**作業の入り口はこの2つです。** 数字に触る前に必ず読んでください。

**リポジトリ側に書類の内容をコピーして置かないこと。** `docs/` は 2026-08-10 に廃止しました。

---

## 2. ★変更したら引き継ぎ書類も直す（例外なし）

**コード・設定・方針を変えたら、引き継ぎ書類の更新までが1つの作業です。**

1. `変更履歴.md` の表に1行足す（**コードと同じコミットで**）。反映列は `未` で出す
2. Dropbox の引き継ぎ書類（上の表のどちらか、または両方）を直す
3. 直したら `変更履歴.md` の反映列を `済` に変えて push する

4. **完了報告を出す**（下の★。**普段は普通の文章**。「貼る用に」と言われたときだけコードブロック1つ）

方針が変わったとき（「この指摘はもう書かない」など）は、**コードが1行も変わらなくても**
書類を直してください。自動チェックでは方針変更を見つけられません。

守られているかは自動で見ています。

- **書き忘れた push** → `docs_guard.yml` が GitHub Actions を赤くし、LINE に通知します
- **`未` が残っている** → 毎日 JST 9時ごろ、`docs_check.py pending` が LINE に思い出させます

### ★ md の更新は担当者にやらせないこと（運営者からの明示の指示）

**書類を直すのはセッション（AI）の仕事です。** 「この内容をコピーして貼ってください」
「このファイルを上書きしてください」と担当者に作業を渡すのは**禁止**です。
フォルダを接続してもらっているのは、そのためです。

Dropbox に届くかはセッションによって変わります。**紐づく先はセッションを始めた時点で決まり、
途中でフォルダを接続してもツールは増えません。** 次のように分けてください。

- **届く場合**（`mcp__remote-devices__*` のファイル操作ツールがある）
  → 自分で直して完了させる。`変更履歴.md` の反映列を `済` にする
- **届かない場合**（ツールが無い／`get_device_info` が「接続されていません」を返す）
  → **書類は直さず、`変更履歴.md` に `未` で積んで次のセッションに渡す。**
  担当者に完成ファイルを渡して差し替えを頼むのは最後の手段（頼むとしても「貼り付け作業」は作らない）

`変更履歴.md` の `未` は**この受け渡しの待ち行列**です。だから消さずに残します。
デスクトップアプリで新しいタスクを「あなたのコンピューター」で始めると、最初から接続済みになります。

### ★ 完了報告の出しかた（2026-08-17 変更）

**普段は普通の文章で報告してください。**何をして、何を確かめて、何が残っているかを短く書きます。

> **2026-08-14〜17 は「常にコードブロック1つ」でした。**運営者がそのままコンソールへ貼るためでしたが、
> 実際には貼らずに自分で読んでいたため、**書式が効かない・中身の無い節にも「なし」と書く・長くなる**の
> 3つが損になっていました。**理由のほうが変わったのでルールを畳みました。**

**「貼る用にください」と言われたときだけ**、報告の全体を md のコードブロック1つに入れます。
そのときの守ること:

- **ブロックは1つだけ。地の文を外に足さない**（分けると貼る手間が増え、外に書いたものは落ちます）
- **ブロックの中に ``` を入れない**（そこで閉じます）。コードは `インラインの記法` で書く
- 見出しは `##` から始める（貼り先で階層が崩れないため）
- **中身が無い節も「なし」と書いて残す**（「書き忘れ」と「無かった」を区別するため）

どちらの形でも守ること: **秘密情報の値は書かない**（§3）。
**やっていないことを「完了」と書かない**（確かめていないものは「未確認」。§4 の4）。

**貼る用の雛形（この6つの見出しを使います）**

```
## ✅ CHANNEL DESK 完了報告（YYYY-MM-DD）

**やったこと** — 1〜2行。何のために何を変えたか

**変更**
- リポジトリ: コミットID / 変えたファイル（コードを触っていないなら「コード変更なし」）
- 書類: どの md の どの章 を直したか

**確認したこと**
- docs_check.py の pending / push の結果
- 実物で確かめたこと（描画・数値・共有設定・書き戻しの一致など）

**判断が要ること** — 運営者に決めてほしいこと。無ければ「なし」

**次にやること** — 次のセッションへの申し送り。無ければ「なし」

**⚠ 注意** — 読み違えると事故になる点。無ければ「なし」
```

---

## 3. 触ってはいけないもの・気をつけること

- **編集前に必ず `git pull`。** `data.json` が3時間おきに自動コミットされます
- `data.json` / `video_daily.json` / `ops.json` は**自動生成**。手で編集しない
  （`data.json` の週次AI分析だけは手順書に従って書き込みます）
- `fetch.py` の `load_existing_data()` は決まったキーだけを読みます。
  **`data.json` に手でキーを足しても翌日の実行で消えます**
- **リポジトリは公開です。** 個人情報を JSON に出さない設計（クーポンのUID・ニックネームなど）を崩さない
- 秘密情報（トークン・APIキー・パスワード）の**値**は、コードにもドキュメントにもコミットメッセージにも書かない
- **Dropbox の書類にある値をリポジトリ側に持ち出さないこと。**（2026-08-10 運営者の判断）
  メールアドレス・LINEグループID・Apps Script の URL・GCP プロジェクトID・ローカルの実パスなどは、
  非公開の Dropbox の書類には書いてよいが、`CLAUDE.md` / `変更履歴.md` / コミットメッセージ /
  コード / Issue には**書かない**。リポジトリは public です
- サイトのパスワードゲートは表示の目隠しです。`data.json` / `ops.json` は raw URL で誰でも読めます
- ローカル確認は `python3 -m http.server` 経由で（`file://` では JSON を読めません）
- クーポンシートのURLは3か所にあります。差し替えるときは全部直すこと
  （`fetch.py` の `COUPON_SHEET_ID` / `index.html` の `card-link` の href / `index.html` の `COUPON_SHEET_URL`）

### push が 403 になるとき

URL に埋め込んだ認証情報はサンドボックスのプロキシに落とされます。ヘッダで渡すと通ります。

```
git -c http.extraHeader="Authorization: Basic $(printf 'x-access-token:%s' "$TOK" | base64 | tr -d '\n')" \
    push https://github.com/ytpokerguild-netizen/channel-desk-data HEAD:main
```

---

## 4. 分析するときの大原則（運営者からの明示の指示）

詳細は Dropbox の `チャンネル分析AI_運用手順.md` にありますが、要点だけ先に書いておきます。
**この5つは他のどの指示よりも優先します。**

1. **頼まれていない分析をしない。** 無駄な分析をするな、という明示の指示があります
2. **置いた前提を全部書く。** 前提を変えると結論が変わるなら、それも書く
3. **判断が要る分岐は自分で決めずに聞く。** 人間より先走らない
4. **既定値は「わからない」。** 判定できないことは「判定できない」と書く
5. **結論を1つに絞らない。** 選択肢と根拠と不確かさを見せる

割合を出すときは必ず分母を書く。日数が違う週を引き算しない。Analytics の確定値は2〜4日遅れる。
