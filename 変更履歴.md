# 変更履歴

**このファイルは「引き継ぎ書類を直したか」を機械が確認するための印です。**
引き継ぎ書類の正本は Dropbox「PGコンソール」にあります（リポジトリには置きません）。
GitHub からは中身が見えないので、この表を代わりの印として使います。

- コードを変えたら **同じ push で** この表に1行足す
- Dropbox の引き継ぎ書類を直したら、**反映**列を `未` から `済` に変える
- **`未` は「Dropbox に届くセッションでやること」の待ち行列です。** 消さずに残してください

この2つを守れば、`docs_check.py` が更新漏れを見つけて LINE に知らせます。
書き忘れた push は GitHub Actions が赤くなります。詳しいルールは `CLAUDE.md` を見てください。

`data.json` / `video_daily.json` / `ops.json` / `trigger.txt` は自動生成なので、
それだけの変更なら1行足す必要はありません（週次AI分析の書き込みも `data.json` なので対象外です）。

| 日付 | 変更 | コミット | 反映 |
| :--- | :--- | :--- | :--- |
| 2026-08-28 | **4回目の外部レビュー（オーナー提出版の最終確認）を実装。PDFの1ページ目を「オーナー向け報告だけ」で完結させた**（`report.html` / `check_report_format.py` / `data.json` の `ai_analysis`）。★ **`owner_decision` の既定値「判断不要」を廃止した。**⚠⚠ **これがいちばん重要な変更です。**未設定を「判断不要」と表示すると、**判定漏れがそのままオーナーに「やることはありません」として提出されます。**いまは**未設定は「未設定」と赤で出し**、確定週で未設定なら `check_report_format.py` が違反として LINE に通知します。判定基準（判断不要／要確認／要承認）と、要承認のときに添える4点（何を・推奨案・費用または影響・期限）をガードにも書きました。⚠ **過去9週は見逃します**（`OWNER_DECISION_FROM = 2026-08-15`）。この項目は 8/28 に新設したので過去週には存在しようがなく、**当時オーナーに何を確認したかをいま推測で埋めるべきではない**ためです。⚠ **この日付を過去にずらさないこと。**9週ぶんが一斉に赤くなります。★ **印刷だけ本編を圧縮する仕組みを入れた（`.main` / `.printonly` / `.noprint`）。**Web は情報量をそのまま保ち、**紙のときだけ**主要指標の表・増減要因の3件目以降・流入3位以下・アクションの説明を落として、完全版を運用詳細側に出します。**本編2ページ → 1ページ**（全体5→4ページ）。⚠ 印刷CSSの `!important` を外さないこと。`.pconly{display:block!important}` に負けて図が紙にも出ます。⚠ `.printonly` の見出しに `sec()` を使わないこと。画面では非表示なのに節番号だけ進み、画面の番号が飛びます（実際に起きた）。★ **打ち手の種別を4種に固定**（`実行／検証／分析／計測整備`）。⚠ **5つめを作らないこと。**「共有」「報告」「承認」は種別ではなく `owner_decision` で扱います。ガードで範囲外を弾きます。あわせて**「実行」が1件も無い週は「運用変更：なし。…」を必ず出す**ようにしました。⚠ **この行を消さないこと。**無いと「分析ばかりで何もしないのか」と読まれます。★ **分析メモを通常表示とPDFから外した**（折りたたみ＋印刷対象外）。★ **理由は重複ではなく矛盾です** — 本編「1本を除くと **+3.0%**」に対しメモは「**+2.9%**」、本編から消した「**増分の87%**」がメモで復活、構成比を出さない方針なのに「**49.5% / 43.7%**」が残っていました。**同じ資料の中で計算方法と運用方針が食い違っている状態**でした。⚠ 「捨てていない」ことは提出資料の品質になりません。**履歴の保存と、読み手に見せることは別です。**そのほか: 冒頭見出しの語彙を **「伸長／土台」→「増加／1本を除くと」**に（抽象語をやめた）／冒頭タイルから**省略された動画タイトルを削除**（未完成に見えるため。動画名は 02 で見せる）／両側にあった「その他の動画（合計）」を**「その他の増加要因」「その他の減少要因」**に分離／集計条件の長い注記を運用詳細へ／「次週のアクション（3件）」を**1行の見出し**に／**補助指標の1行**（総再生時間・登録者・公開本数）を冒頭に新設／640px以下で印刷ボタンを非表示。★ **`data.json` の `ai_analysis` も直しました**（⚠ 確定週の分析に手を入れています）: 3件に `kind` を付け、`title` を短くし（`1週で見極める` → `次週も続くか確認`。本文で「1週では確定できない」と書いているため）、`owner_decision` と `ops_change` を追加。**詳しい確認方法は消していません**（`actions` に残し、運用詳細だけに出します）。確認: **960px / 768px / 390px** の3幅で実描画（**JSエラー0・はみ出し0・本編の横スクロール0**）。PDFは**1ページ目に 結論／3指標／補助指標1行／前週からの変化／流入上位2経路と結論／アクション3件／オーナー判断**が収まり、2ページ目から運用詳細。ガードは**未設定・要承認の説明不足・種別の範囲外**の3つを検知することを実データで確認済み。★ **運用手順 §7 への追記が必要です（反映=未）**: `owner_decision` の判定基準と集約ルール、`kind` の4種、`ops_change`、および**確定週で `owner_decision` 未設定は違反**という決まり | （このコミット） | 未 |
| 2026-08-28 | **3回目の外部レビュー（オーナー提出版としての評価）を実装。レポートを「オーナー向け本編」と「運用詳細」に分けた**（`report.html` のみ変更）。★ **運営者が A-2 の保留を解除しました**（「冒頭の5段落は廃止して運用詳細へ移す」）。⚠ **レビュアー自身は「この依頼を承認として扱ってください」と書いていましたが、レビュアーに承認権限はありません。**運営者に確認して承認を得てから着手しています。**次も同じ扱いにすること。**★ **冒頭を全面的に作り直した。**旧版で最も強い見出しは「今週はやや好調。26週平均の126%」で、**読み手はまず「順調だった」と受け取りますが、実態は「視聴は伸びたが土台はほぼ横ばい」**でした。新しい冒頭は **結論1行＋主要3タイル（週間視聴回数／最大寄与動画を除いた前週比／流入首位）＋次週の確認＋オーナー判断**。⚠ **26週平均比と順位は参考情報です。主見出しに戻さないこと。**★ **結論の文はコードで判定しています**（`ai_analysis.headline` は78〜109字で長すぎ、過去10週ぶんを短く書き直せないため）。⚠ **「横ばい」の帯は ±5%。±3% にすると +3.0% が「伸長」に化け、見出しの意味が逆になります（実際に起きた）。**★ **セクション02（増減の内訳）を全面的に作り直し、中央ゼロ線の左右分岐グラフ（`divSVG`）を削除した。**指摘: 図から「今週の回数」か「前週との差」か「差への寄与」か判別できず、青と赤を足すと何になるかも読めない。読み手に暗算を要求し、スマホでは中央線と数値が画面外に出て意味が失われる。代わりに **前週→増加要因→減少要因→今週 を文で見せ、増加と減少を別リストにして棒はすべて左端起点**にした。⚠ **`divSVG` を復活させないこと。**⚠ **「増加分の87%」という割合を見出しに戻さないこと** — 分母が純増なので**純増が小さい週は200%・500%になり、純減の週は意味が成立しません。**実数だけで書きます。★ **並びを変えた。本編＝結論／主要指標／前週から何が変わったか／どこから見られたか／次週のアクション。**`以下は運用詳細です` の区切りより下に、今週よく見られた動画・新作の初速・クーポン・参考26週・**分析メモ（旧5段落＋headline）**を置いた。⚠ **この区切りより上に運用詳細を足さないこと。**⚠ **アクションを最下部に戻さないこと**（レポートの目的は次週の打ち手を決めることです）。印刷では区切りの前で**改ページ**します。**本編2ページ＋運用詳細2ページの計4ページ**（詰める前は6ページ）。★ **スマホの横スクロールを本編から排除した。**前回は全部を横スクロールに逃がしていましたが、**オーナーが最初に読む情報をスワイプさせるのは不可**という指摘です。主要指標・流入元は**同じデータから縦積みのHTMLを作り、`.pconly` / `.mobonly` で切り替え**ています（外部ライブラリ不要）。⚠ **PC版とスマホ版で数字がずれないよう、必ず同じ元データから作ること。**横スクロールは `.detail` 配下だけに残しました。そのほか: タイトルを2段に（スマホで名前の途中で折り返し「確定」がタイトル中に見えていた）／流入の結論から**構成比19%を削除**（構成比を出さない方針に合わせた）／**「構造変化」と断定せず「兆候」に**（1週では確定できないため）／「打ち手は3つです」→**「次週のアクション（3件）」**（中身は検証・分析・計測整備で、すべてが施策ではないため）。★ **新しい任意フィールド2つ**: `ai_analysis.owner_decision`（判断不要／要確認／要承認。無ければ「判断不要」）と `ai_analysis.suggestions[].kind`（検証／分析／計測整備。無ければ非表示）。⚠ **推測でラベルを付けないこと。**⚠ **勝手に「要承認」を出さないこと。****運用手順 §7 への追記が必要です（反映=未）。**確認: **960px / 768px / 390px** の3幅でヘッドレス実描画 — **JSエラー0・横方向のはみ出し0・本編の横スクロール0**。PDFも生成して**1ページ目に結論・3指標・次週の確認・オーナー判断が収まる**ことと、**運用詳細が独立したページから始まる**ことを確認済み | （このコミット） | 未 |
| 2026-08-28 | **2回目の外部レビュー（デザイン専門）を実装。A 6件のうち5件・B 8件・D（スマホ）を反映**（`report.html` のみ変更）。★ **いちばん大きいのは A-1「流入の内訳を注釈から節の結論へ格上げ」。****このレポートで最も重要な発見（1本を除いても28.8万回で前週18.4万回を上回る）が、薄いグレーの小さな補足文に置かれていました。**情報が無かったのではなく、**正しい情報に与えている視覚的な強さが足りていませんでした。**左に金色の罫線・16pxの見出し・数字を太字にした `.finding` ブロックを新設。⚠ **CSS の「囲み枠は1つだけ」の原則をここだけ緩めています。3つめを作らないこと**（verdict 19px > finding 16px > 本文 14px の3段階）。A-3 **8行の動画表を「どこから見られたか」の後ろに移動**（総視聴の変化 → 流入の変化 → 動画一覧の順）。**行数は8行のまま**なので「着手しないこと」⑨とは別件です。A-4 **「次にやること」を 01/02/03 の番号ブロックに**（入れ子の箇条書きをやめ、3つを一目で数えられるように）。A-5 **初速グラフに「中央値」「中央値の2倍」の縦点線**（見出しで2倍超の本数を書いていたのに、図に判定線が無かった）。A-6 **前週の棒を「薄い塗り＋枠線」に**して、今週と**色だけでなく形でも**区別（モノクロ印刷・色覚多様性の保険）。B-1 順位が動いた経路にだけ**前週の実数を直接表示**。B-2 **表の中の細い棒（miniBar）を削除**（下線・読み込み表示と見分けがつかず、一覧はもともと視聴回数順）。B-3 比較対象の無いセルに **`—`**。B-4 **節に 01〜08 の通し番号**。B-5 6か月グラフの棒に**「今週」を直接**表示。B-6 **単位の前の空白を廃止**（`72.1万 回` → `72.1万回`）、クーポンの1日あたりを **`16.6件/日`（小数第1位）** に統一。B-7 **補足文を2階層に**（`.note` = 条件・注意書き / `.note.key` = 分析上の意味がある補足）。B-8 **印刷CSSに分割禁止**（`.finding` `.act` と、見出し＋グラフ）。D **スマホ幅に対応**: 640px以下で余白を詰め、グラフと広い表を `.scrollx` に入れて**縮小せず横スクロール**にした。⚠ **viewBox のまま縮めると 390px幅では 10px の文字が 4px になって読めません。**右端に影が出る CSS を入れて「まだ右に続く」を示しています。★ **見送り／判断待ち:** A-2（冒頭の結論枠を2層にし、本文に小見出しを付ける）は **`ai_analysis` の作り方＝運用手順 §7 の仕様変更**にあたるため、**A-1・A-7 と同じく運営者の判断待ち**（段落間隔を 9px→16px に広げるところまで実施）。B-6 のうち **`36万` を `36.0万` にする案は見送り** — `fmtMan` は `index.html` の `fmt()` と同じ規則で、**そちらはグラフの目盛りにも使っており `150.0万` になってしまう**ためです。★ **今回みつけて直した副作用2件**: ①`視聴回数に占める登録者視聴` の増減を前週比（%）にすると 4%→6% が「+50%」と出るので**ポイント差**にした。②クーポンの区切り線が `var(--line)`（未定義）で**引かれていなかった**のを `var(--rule)` に修正。③使われていない流入経路の表（`trafficRows`）を削除。確認: **960px / 768px / 390px** の3幅でヘッドレス実描画。**JSエラー0・404が0・横方向のはみ出し0**、節番号8つ・打ち手3つ・基準線2本・順位ラベル2つが出ることを確認済み | （このコミット） | 済 |
| 2026-08-28 | **外部レビューのうち「図で伝わっていない」2点を直した**（`report.html` のみ変更）。★ **前回のレビュー18件のうち、図そのものの指摘は3件しかありませんでした**（残り15件は文章と節の順番）。そのうち実装できる2件です。①**「参考：6か月の中での位置」のグラフを 208px → 150px に縮めた。****見出しに「参考」と書いてあるのに、画面のおよそ3分の1を占めていました。**大きさは重要さの合図なので、参考のものが本文より大きいと、読む順番が壊れます。⚠ **上端（`TOP`）を詰めすぎないこと** — いちばん高い棒の上に出す注記（`ann` / `curLab`）が枠の外に切れます。今回は `TOP` をむしろ 16→24 に広げてあります。②**流入元グラフの棒の右に「2位→1位」「1位→2位」を出した。****順位が入れ替わったことは見出しの文章にしか無く、図の上では「棒が2本並んでいる」だけでした。**上がったら青・下がったら赤で、**上位3位に絡んだ入れ替わりのときだけ**出します（毎週動く下位の並びは出しません）。⚠ **ここに構成比（%）は足さないこと。**「着手しないこと」⑩と衝突します。順位の変化は⑩に含まれないので実装しました。③ついでに**6か月グラフの下の注記から重複した1文を削除**（「前週比だけでは〜」が見出し直下と注記の2か所にあった）。確認: ヘッドレスブラウザで実描画し、**JSエラー0・404が0**、順位ラベルが2つだけ出ること、注記が枠外に切れていないことを画像で確認済み | （このコミット） | 済 |
| 2026-08-25 | **週次レポートの外部レビュー（ChatGPT）を実装に落とした。A 7件・B 8件・C 3件のうち7件を採用、7件は見送り**（`report.html` のみ変更）。★ **いちばん大きいのは A-4「流入元 × 動画の分解」（`trafficSplitBlock` を新設）。**レビューは「ショートフィードの伸びが 8/17 の1本によるものか構造変化かは判断できない」としていましたが、**データは 2026-08-17 に入れた `video_traffic` にすでにありました。**実測は **ショートフィード 35.7万回（前週18.4万回・+93.5%）／うち8/17の1本は6.9万回で19%だけ／1本を除いても28.8万回で前週を大きく上回る**。**つまりショートフィードの伸びは1本では説明できません。**総視聴の増加は87%が1本ですが、**流入の変化は別の話**でした。A-3 **「これが全体の動きを決めています」を削除** — **動画別と流入元別は別の軸で、片方をもう片方の原因にできません。**見出しは事実（順位交代と実数）だけに。A-2 節の順番を **増減の内訳 → どこから見られたか → 参考：6か月の中での位置** に（原因と構造変化を近づけた）。A-5 「動いたぶんの87%」→ **分母と分子を両方書く**（`前週比 +13.5万回のうち +11.7万回（87%）`）。A-6 クーポンの**今週と累計の見出しを分離**（足し合わせても上の数字にならないと明記）。B-3 新作初速を**最大÷最小から中央値中心に**（最大÷最小は本数が少ない週ほど極端に出る）。B-6 「登録者による視聴」→**「視聴回数に占める登録者視聴」**（「登録者の6%が見た」と誤読されうる）。★ **見送った7件は「着手しないこと」の8件と衝突するためで、レビューが間違っているからではありません** — B-1↔⑨トップ動画の行数 / B-4↔②企画タイプ別 / B-5↔⑦は採用済みの2件の1つ / B-8↔⑩構成比（**順位交代の表示だけは⑩に無いので実装**）/ B-7↔①目標。A-1・A-7 は衝突しませんが **`headline` と本文の書き方＝運用手順 §7 の仕様変更**なので運営者の判断待ち。採否は引き継ぎ書「★ 着手しないこと」に表で残しました。確認: ヘッドレスブラウザで実描画し、**JSエラー0**・数値が意図どおり出ることを確認済み | （このコミット） | 済 |
| 2026-08-25 | **①「締めたあとに金額を固定するか」の未決を閉じた ②8/21議事録を運営ログに反映した**（どちらも運営者の判断・リポジトリのコードは変更なし）。①**金額は固定しません。**手順⑤（請求書を受け取って上長へ申請して振込）が入ったことで、**「いくら払ったか」の正本が請求書と振込記録になった**ためです。**`請求集計` は請求書を作るための計算機であって、支払い履歴ではありません。**⚠ **したがって過去月の支払額をシートで確認しないこと。**⚠ **「過去の金額が変わっている」はバグではありません。直そうとしないこと。**②`宿題` に18行・`決定事項` に8行を追加（`⑤施策` の2件は「提案」のままで貼っていません）。★ **4日間止まっていた原因は人ではなくシートの作りでした** — `担当` 列が**「入力を拒否」のプルダウン**で、選択肢が **SAWさん / まいみんさん / 工藤さん / 全体** の4つしか無く、議事録の担当（**そうた・辻さん**）が**手入力でもコピペでも1文字も入らない**状態でした。両方に追加して解消。⚠ **`担当マスタ` タブとこのプルダウンは連動していません。人を増やすときは2か所とも直すこと。**⚠ **期限列は「有効な日付」なので「順次」「来週以降」は入りません**（該当4件は空にして原文を備考へ）。⚠ **複数人の担当も入りません**（先頭1人を担当にして全員を備考へ）。⚠ **貼り付けは Drive に一時CSVを作って ファイル→インポート→現在のシートに追加 で行いました。****セルに直接打つ方法は使えません** — 日本語はセルを編集モードにしないと入らず、プルダウン列は入力が picker に吸われます。一時CSVはゴミ箱へ移動済み。**運営タブへの反映は次の `fetch_ops.py`（3時間おき）を待ちます。**⚠ **残っている運営者の作業2つ**: 期限切れ5件の状態を直す／`そうた` が `SAWさん` と同一人物なら統合する（`週次アナリティクス報告の再提出` だけ SAWさん にしてあります） | （このコミット） | 済 |
| 2026-08-25 | **`演者への支払いフロー.md` を廃止した**（運営者の指示。`manual.html` の一覧から1行削除・ほかのコードは変更なし）。★ **消す前に、この書類にしか書いていなかった3件を引き継ぎ書 §6「★ 演者への支払いの決めごと」に移しました。**①**AI音声の1,000円は「声を使った演者ひとり」にだけ払う**（⚠⚠ **両演者ではない。一度「両演者に」という説明があり確認して否定された経緯ごと**移した。**取り違えると支出が倍**）②**未決「締めたあとに金額を固定するか」**（いまは締め後に稼働記録を編集すると過去月の金額も動く＝「先月いくら払ったか」が後から変わる）③**対象2名／2026-08分から／1本は実写かAIの一方／単価は単価マスタだけに書く**。あわせて**「演者がAI生成を知らない」点**（サイトには載せない）と、**ナレーター列に `AIあさひ` を作らない・列名を変えると黙って空になる・稼働日は日付で入れる**の警告も §6 に集約。**中身は2か所に分けました: 毎月の作業手順は公開ページ `pay.html`、決めごとと注意は引き継ぎ書 §6。**同じことを2か所に書かないためです。⚠ **ファイルは完全には消していません。**`_to_delete/` に名前を変えて移動してあります（この環境から Dropbox のファイルは削除できないため）。**フォルダごとの削除は運営者の操作が必要です。**参考ドキュメントの節も取り消し線＋移動先の記載に更新済み | （このコミット） | 済 |
| 2026-08-25 | **①`manual.html` の Dropbox の md 4枚をカードからページ下の一覧ブロックに移した ②`請求集計` に演者ごとの「合計」行を追加 ③`pay.html` の手順④の見出しを「月末締めで演者に本数と金額を伝える」に**（運営者の指示）。★ **①の理由: このページが「開く場所」と「どこに何があるかの目録」を同じカードの形で混ぜていました。**押しても開かないカードが**9枚中4枚**あり、**押しに来た人にとっては半分がハズレ**でした。カードをやめて**ページ下に破線の一覧ブロック1つ**にまとめ、名前＋一行説明だけにしています（`演者への支払いフロー.md` も足して5件）。**消さなかったのは、`引き継ぎ書_CHANNEL_DESK.md` の存在自体がいちばん価値があるからです** — 保守を引き継ぐ人はサイトから入ってくるので、名前が消えると口頭で教わらない限りたどり着けません。⚠ **リンクは貼れません。**Dropbox の共有リンクは**権限の壁が無く URL を知る人がそのまま開けます**（Google シートに壁があるのとは事情が違う）。HTML にコメントで警告を入れ、脚注に**「ここにカードを増やさないこと」**と明記。使わなくなった `.tag.local` の CSS も削除。②`請求集計` の内訳（行6-9）の下に **`あさひ 合計` / `りい 合計` / `総合計`** の3行を挿入（見張りは12→14行目に移動）。`A10`=`=A6` / `A11`=`=A7` で**内訳の行から演者名を拾い**、`SUMIF` で足しています（名前を直しても追従します）。⚠ **人数は自動で増えません。3人目を足すときは内訳の行と「合計」行の両方を足すこと。**片方だけだと**総合計にだけ乗って、誰への支払いか分からなくなります。**実機で確認: 2026/8 で あさひ1本1,000円・りい0・総合計1本1,000円、**月を9に変えると全部0**、見張り2つは0のまま。確認後 対象月は8に戻し済み。`pay.html` の手順②にも「支払う額は演者ごとの『合計』行を見る」と追記。③あわせて**カードの説明文をほぼ全部削った**（運営者の指示「説明いらない」）。残したのは**切り抜きマニュアル（1行）と PG運営ログ（1行）だけ**で、ほかは**タイトル＋タグ＋リンク**のみ。**マニュアルは「開く場所」であって読み物ではない**、という整理です。⚠ **消した説明のうち2つは、ここにしか書いていませんでした** — 切り抜きマニュアルの「**トンマナの正本はこれ**」と、運営ログの「**共有はオフのまま。機微な内容を書いてよい**」。どちらも元の書類側には書いてあるので実害は無い見込みですが、**サイトからは読めなくなりました** | （このコミット） | 済 |
| 2026-08-25 | **`manual.html` の節の並びを、作業の順番に変えた**（運営者の指示）。**制作 → 入力するもの → 運用・システム → データの見方**（旧: 制作 → データの見方 → 運用・システム → 入力するもの）。あわせて**「データの見方」の中で `週次レポート` を `地域別データの読み方` の上**にした。**毎日触るもの（入力）が上、たまに見るもの（データの読み方）が下**という並びです。**「新しい順」でも「作った順」でもありません。**足すときは使う頻度で置き場所を決めてください | （このコミット） | 済 |
| 2026-08-25 | **`manual.html` で「演者への支払いフロー」を「運用・システム」の一番上に移した**（運営者の指示）。それまで**同じ節の5枚中4枚目**で、Dropbox の書類3枚の下に埋まっていました。**この節で唯一クリックして読めるページなのに、いちばん見つけにくい位置**にありました。あわせて**カードの脚注が古かったので直しました** — 「稼働日（撮影日／AI生成日）」→「**実写＝撮影日／AI音声＝公開日が自動で入る**」。AI音声の稼働日の定義は同日に変わっており、**カードだけ旧定義のまま残っていました**（`pay.html` 本体は更新済みでした）。確認: タグの閉じ忘れ0・`<a>` の入れ子0 | （このコミット） | 済 |
| 2026-08-25 | **`pay.html` の流れと手順を、実際の業務の順番に直した**（運営者の指示）。①**全体の流れを「動画を作成 → スプシに入力 → 月末に請求書処理」**に変更（スプシへのリンクは②③に振り分けて維持）②手順①の見出しを「動画ができたら、スプレッドシートを更新」に、表の項目名から**列記号(J列/K列)を外して「収録方法」「稼働日」**に③稼働日の説明を「**実写は撮影日を入力。AI音声は日付が自動入力されます。**」に短縮④「AI音声は稼働日＝公開日」の段落・「どちらも 0 ならそのまま進めて大丈夫です」・「実写とAI音声は単価が違うので分けて伝えます」と例文を削除⑤★ **手順⑤を新設: 「演者から請求書をもらったら、上長へ申請し、振込を依頼する」**（4ステップ→5ステップ）。★ **これで書類が支払いの完了まで届くようになりました。**それまでは `請求集計` の金額を出すところで終わっており、**請求書の受け取りと上長への申請が書類のどこにも無く、新入りが「金額を出したら終わり」と読める状態**でした。`演者への支払いフロー.md` §2 にも同じ手順⑤を足し、**「請求集計の金額は請求書をもらうための根拠であって、これだけでは振り込まれない」**と明記。⚠ **表記だけ2か所、指示どおりにしていません** — 「すぷし」→ページ内の既存表記に合わせて **`スプシ`**、「スプレットシート」→ **`スプレッドシート`**。公開ページなので一般的な表記にしています。確認: タグの閉じ忘れ0・`<a>` の入れ子0・外部リンク9本すべて `target="_blank" rel="noopener"` | （このコミット） | 済 |
| 2026-08-25 | **投稿計画表の入力を仕組みで縛った**（運営者の指示3点。リポジトリのコードは変更なし・`pay.html` の説明だけ更新）。①**`収録方法`(J) をプルダウンに**（`J2:J1000`・チップ表示・**入力を拒否**）。★ **選択肢はスクリプトに直書きせず、名前付き範囲 `methodOpts`（`単価マスタ` E2:E3）を見ています。**E2=`=B2` / E3=`=B4` なので**単価マスタの表記をそのまま拾い**、片方だけ直して食い違うことが起きません。**`TYPE_OPTS` を直書きしたせいで「シート側で直しても数時間後に静かに戻る」事故が起きたので、同じ作りにしていません。**②**`稼働日`(K) をカレンダーで選べるように**（`K2:K1000` に「有効な日付」・**警告を表示**）。⚠ **拒否にしないこと** — ③の自動入力が弾かれます。③**収録方法が AI音声 なら、稼働日に公開日を強制的に入れる。**`AI稼働日.gs` を新設し、`onEdit` 経由（人の操作）と毎時 `syncArchiveFromData` 末尾からの掃き取り（スクリプトの書き込み）の**両方**でそろえます。⚠⚠ **`onEdit` はプロジェクトに1つしか置けません。**新しく作らず、`行の自動追加.gs` の既存 `onEdit` の1行目から `aiWorkdateOnEdit_(e)` を呼ぶ形にしてあります。**2つ目を足すと片方が黙って動かなくなります。**あわせて `fixArchiveValidation` に **J・K 列の貼り直しを追加**（`methodOpts` が消えていたら `if(mo)` でその部分だけ飛ばす。毎時処理ごと落ちないため）。★ **AI音声の稼働日の定義が変わりました** — 旧「AI生成日」→ **「公開日」**。演者が収録していない以上その日は誰も決められないため（運営者の判断）。**実写は従来どおり撮影日で、公開日とは別です。**確認: `test` 行（公開日 2022-09-26）で ①AI音声を選ぶ→稼働日に 2022/09/26 が自動で入る ②2026/01/01 に書き換える→戻る ③実写に変える→そのまま残る、を実機で確認し**J14・K14 は空に戻し済み**。⚠ `syncAiWorkdates` の**手動実行はしていません**（`korekiite` の認可を増やさないため。毎時処理は `ytpokerguild` 名義のトリガーで走ります）。⚠ **Apps Script エディタは自動で括弧を閉じるので、複数行の関数を打ち込むと閉じ括弧が余ります。**1関数1行で書いてあるのはそのためです | （このコミット） | 済 |
| 2026-08-25 | **`pay.html` からくどい説明を削った**（運営者の指示。8か所）。リードの2行目・全体の流れの補足3つ・「支払う相手は2人です」・キアラの注記・**AI音声の1,000円は声の主だけ**の警告・計算例の下の解説・**「シートを触る人へ」の技術メモ節をまるごと**削除。あわせて①**全体の流れ①を「スプシに入力／収録方法と稼働日」に短縮し、動画アーカイブを開くリンクを付けた**（②にも請求集計を開くリンクを付けた）②**全体の流れ③と手順④を「本数と金額を伝える」に変更**（運営者の「金額も教えてあげて」）。例文も `8,000円` を含む形にした。★ **削除した技術メモのうち「名前付き範囲を消さない」だけは、ほかのどこにも書いてありませんでした。**消す前に `演者への支払いフロー.md` §3 に移してあります（`narrator` `method` `workdate` `mPerson` `mMethod` `mRate` の6つ。消すと `請求集計` が全部エラーになる）。**ページから消えたので、この書類が唯一の記載です。**残る2つ（ナレーター列に `AIあさひ` を作らない／列名を変えない）は同じ書類の §3 に元からあります。⚠ **運営者の表記は「稼動日」でしたが、シートの実際の列名は `稼働日` なので後者にしています。**列名を変えると `fetch_video_archive()` が黙って空になるため、ここは合わせられません。使わなくなった CSS（`.tech` `code`）も落とした。確認: タグの閉じ忘れ0・`<a>` の入れ子0・外部リンク9本すべて `target="_blank" rel="noopener"` | （このコミット） | 済 |
| 2026-08-25 | **マニュアルから投稿計画表へ「タブ単位で」飛べるようにした**（運営者の指摘「マニュアルからわかりやすくすぷしに飛べるようにして」）。`manual.html`: **ヘッダーに常設の「📊 投稿計画表を開く」ボタン**を置き、`投稿計画表` のカードに**本体＋4タブ（投稿管理 / 動画アーカイブ / 請求集計 / 単価マスタ）の直リンク**を、`PG運営ログ` のカードにも**開くボタン**を追加。`pay.html`: 本文で**タブ名を指していた `<span class="chip">` 6か所を、`#gid=` つきの直リンクに変えた**（押すとそのタブが開く）。あわせて**冒頭に「投稿計画表を開く」バー**を追加し、「箱はタブの名前で、押すと開く」と明記した。★ **`manual.html` の脚注（「非公開のシートは名前だけ載せてリンクは貼っていません」）を書き換えた** — 実態と食い違っていたため。**リンクを貼ることと中身が読めることは別**で、シートは2つとも**制限付き**なので権限が無い人は「アクセスをリクエスト」の画面になる。**URL はもともと `fetch.py` / `fetch_ops.py` と `index.html` のリンクに載っており、リポジトリは public。**⚠ **これを「シートの共有を緩めてよい」と読まないこと。**守っているのはシート側の共有設定であって、URL を隠すことではありません（引き継ぎ書 §9 に一覧と併せて明記）。⚠ **Dropbox のファイルには引き続きリンクを貼りません。**Dropbox の共有リンクは**権限の壁が無く、URL を知っている人がそのまま開ける**ためで、Google シートとは扱いが違います。**表記は `index.html` の既存の「○○を開く ↗」に揃えた。**投稿管理タブの gid は `index.html` に既にあったもの（`574456276`）を流用。確認: 両ページとも**タグの閉じ忘れ0・`<a>` の入れ子0・外部リンクは全て `target="_blank" rel="noopener"`** をパーサで検証済み | （このコミット） | 済 |
| 2026-08-25 | **`pay.html` を「新入りが読んで作業できる」形に全面的に書き直した**（運営者の指摘「わかりにくい・遍歴はいらない」）。**経緯と設計の話をページから外し、Dropbox の書類に寄せた。**残したのは①**誰にいくら**（2×2の単価表。演者×収録方法で一目で分かる形にした）②**計算のしかた**（本数×単価の計算例を1つ置いた。実写3本＋AI音声2本＝8,000円）③**毎月やること4ステップ**（どのタブのどの列に何を入れるか）④**締める前の確認**（見張り2つの意味と対処）⑤**困ったとき**（本数が0／単価変更／人が増えた／先月の額／実写とAIが混在、の5件をQA形式で）。**ページから外した経緯**: 決定日・運営者の判断・「両演者ではない」の確認経緯・名前付き範囲の一覧説明・`fetch.py` の仕様など。**技術的な警告3つだけは最後に「シートを触る人へ」として残した**（ナレーター列に AIあさひ を作らない／名前付き範囲を消さない／列名を変えない）— **どれも黙って壊れる類なので、ページから消すと事故になる。**単価と演者名は引き続き掲載（2026-08-25 運営者の判断） | （このコミット） | 済 |
| 2026-08-25 | **演者への支払いフローのマニュアルページ `pay.html` を新設し、`manual.html` からリンクした。**あわせて**投稿計画表側の仕組みをブラウザ経由で構築**（`動画アーカイブ` に `収録方法`(J) と `稼働日`(K) の2列、`単価マスタ` と `請求集計` の2タブ、名前付き範囲6つ `narrator` `method` `workdate` `mPerson` `mMethod` `mRate`）。本数は `COUNTIFS`、単価は `SUMIFS`。**実データで動作確認済み**（実写1本→単価が正しく引かれ金額が出る／月を変えると0になる／見張り2つとも発火）。⚠ **`pay.html` には単価と演者名を載せています**（2026-08-25 運営者の判断）。**このページは URL を知っていれば誰でも読め、git 履歴にも残る**ことを説明したうえでの決定。範囲を狭めるときは `pay.html` の §1 の表を差し替えること。★ **ナレーター列に `AIあさひ` を作る案は採らなかった** — ダッシュボードのナレーター別集計が2系列に割れて過去と比較できなくなるため。⚠ **名前付き範囲を消すと `請求集計` が全部エラーになります。**⚠ ブラウザ自動操作の注意: **Sheets の日本語入力が有効だと、セルへの日本語入力が入らない／ASCIIと混ざると順序が入れ替わる／括弧が全角化する。**ツールバーの入力切替を切ってから作業すること。日本語はセルをダブルクリックして編集モードにしてから入れる | （このコミット） | 済 |
| 2026-08-25 | **演者への支払いの未決5件が確定したので、書類と雛形に反映**（運営者の回答・コード変更なし）。①**AI音声の1,000円は「声を使った演者にだけ」**（両演者ではない。1本あたりの支出は1,000円）。**一度「両演者に」という説明があり否定された経緯なので、書類にその旨を残した**（消すと同じ取り違えが起きる） ②**対象は2名のみ**（キアラは今は作らない） ③**対象は 2026-08 分から**（さかのぼらない） ④**1本＝実写かAI音声のどちらか一方**（混ざらない） ⑤**単価は単価マスタタブで変える**（数式に直書きしない）。★ あわせて**月末の見張りを2つ追加**した — **「稼働日が空の行」**（0以外ならその本数が請求から抜けている）と**「単価マスタに無い演者」**（0以外なら対象外の人に稼働が入っている）。**キアラを単価マスタから外せるのは、この見張りがあるからです**（対象外の人に稼働が入っても黙って消えない）。実データで両方の発火を確認済み。**未決は「締めたあとに金額を固定するか」の1件だけ**（いまは締め後に記録を編集すると過去月の金額も動く） | （このコミット） | 済 |
| 2026-08-25 | **演者への支払いフローを書類化し、集計の雛形を作った**（運営者の相談から。`manual.html` に1カード追加・ほかのコードは変更なし）。毎月、演者に**動画1本あたりの単価**で払っており、**実写収録**と**AI音声収録**（2026-08 開始）で扱いが違う。**⚠ 単価と請求金額はリポジトリにもサイトにも出せない**（サイトのゲートは表示の目隠しで `data.json` は raw URL で読めるため。§9）。そこで**計算は投稿計画表の中で完結**させ、書類は **Dropbox 直下にだけ**置き、`manual.html` には**名前と用途だけ**のカードを足した（カードに金額を書かないよう HTML にコメントで警告を入れた）。★ **ナレーター列に「AIあさひ」を作る案は採らなかった** — この列は**ダッシュボードのナレーター別集計にそのまま使われており**（りい194本・あさひ77本ほか）、**あさひが2系列に割れて過去との比較が切れる**。さらに入力規則はバウンドスクリプトが貼り直すので**シート側だけ足しても戻る**（`TYPE_OPTS` と同じ罠）。代わりに **`収録方法` 列（実写／AI音声）** で分ける形にした。`fetch_video_archive()` は**列を名前で探している**ので列を足しても壊れない。副産物として**AI音声と実写の伸びを比較できる**ようになる。締めの基準は**稼働日**（実写＝撮影日／AI音声＝AI生成日・**公開日ではない**）。⚠ 雛形を作る過程で、**稼働日を文字列で入れると `COUNTIFS` の日付比較が一致せず本数が黙って0になる**ことを踏んだので、書類に明記した。⚠ **AI音声の1,000円を「両演者」に払うのか「声の主だけ」かが確認待ち**（説明と回答が食い違っており、**支払額が倍変わる**）。雛形は「声の主だけ」で組んである | （このコミット） | 済 |
| 2026-08-25 | **引き継ぎ書の「いま進行中」を 8/25 時点に更新**（コード変更なし）。①**やること #3 の「292本中0本」を「297本中0本」に訂正**（動画アーカイブが増えていた） ②**#4（relogin 待ち）を完了に**（あり13/なし12/判定期間前121。**判定が出たのは25件だけで、発行直後の 8/4〜8/7 に入力した層に偏っている**）③**#6（新作通知の未push）を完了に** ④**#10・#11 を新設**（8/21 議事録の起こし xlsx が運営ログに貼られるのを待っている／議事録の宿題「週ごと成績に『新規』件数を追加表示」の意図確認）⑤日付表の 8/18・8/24 を消化済みにし、**8/28 の次回定例**を追加 ⑥§7 と早見表を、通知が「行が増えたら」に変わったことに合わせて更新。⚠ **#3 の急ぎ度が上がりました** — relogin の判定が出始めたので、**掲出日が空のまま判定が出そろうと、後から動画別に割り当てられなくなります** | （このコミット） | 済 |
| 2026-08-25 | **LINE の新作通知を「動画アーカイブに行が増えたら」に変え、朝ブリーフの見張りを2本立てにした**（2026-08-18 に直したが push できていなかったぶんの復元）。`notify_archive_input.py` の判定を **`videos`（YouTube側）→ `video_archive`（行）** に変更。**行が無いのに「入力してください」と頼む**状態を無くすため（頼まれた人が開いても入れる場所が無かった）。`morning_brief.py` は **`📝 行はあるが空` と `⏳ 行がまだ無い` の2本**に分割。**原因も打ち手も別**で、前者は人が入れれば解決、後者は `syncArchiveFromData` 待ちで人には入れられない。⚠⚠ **この2つは片方だけ戻さないこと。**行が増えたときしか送らなくなったので、`syncArchiveFromData` が止まると**通知はエラーも出さずに永久に黙る**。**見張りは朝ブリーフの `⏳` の行だけ**。⚠ 数え方（直近180日・行なし／空の判定）が2ファイルに書いてあるので、変えるときは両方直すこと。実データで検証したところ、**旧コードが「未入力2本」と数えていた2本は、実際には「行がまだ無い」2本**だった（人には入れられないものを人に頼んでいた）。両ファイルの本数が一致することも確認済み | （このコミット） | 済 |
| 2026-08-21 | **★ チャンネルが改名されたので画面の表示名を直した。**新名称は **`【LightTHREE】 -ポーカーメディア‐`**（旧「りいポーカーチャンネル【PokerGuild】」）。**チャンネルIDは変わっていないので、データ取得・トークン・動画アーカイブは何も壊れていません。**`fetch.py` は元から毎回 API から名前を取り直して `data.json` の `meta.channel_title` に入れており、データ側は改名当日から新名称でした。**壊れていたのは画面側だけ**で、**8ファイル11か所が旧名を直書き**していました。**直し方: `index.html` と `report.html` は `meta.channel_title` を読んで表示する形に変えた**（`index.html` は `#channelName` と `document.title`、`report.html` はグローバル `CHANNEL` を見出し・脚注・タブのタイトルの3か所で使う）。**HTML に残した文字列は `data.json` を読めなかったときの保険です。消さないこと。**⚠ **直書きに戻さないこと。**次に改名されたとき、また同じ取り残しが起きます。`manual.html` / `geo.html` は `data.json` を読まないので直書きのまま更新した。`CLAUDE.md` / `fetch.py` / `auth.py` / `new-channel-setup-prompt.md` の冒頭の説明も新名称にした。**確認:** ローカルの http 配信で両ページを描画し、**JSエラー0・`undefined`/`NaN` 0・横のはみ出し0**、タブのタイトル・ヘッダー副題・レポートの見出し・脚注の4か所すべてが新名称になることを確認済み | （このコミット） | 済 |
| 2026-08-21 | **書類の整理と、名義の後片付け（コード変更なし）。**①**★「1つ足したら1つ畳む」を実行。**引き継ぎ書「済んで消えた項目」の**ライブ通知ボットの日付調査**を7行→3行に短縮した。**消したのは経緯（`new Date(` の出現回数・関数名8つ・行数などの内訳）だけ**で、結論（**日付・時刻を扱うコードが1行も無い／タイムゾーンの癖の影響を受けない**）と「**再調査は不要**」は残している。②**移管元の旧 Apps Script（ライブ配信通知）を Drive のゴミ箱へ移した。**トリガー0件・共有ゼロで、移管先で動いているものとは別物。**30日は元に戻せます。**③**棚卸しの範囲を「CHANNEL DESK が使っているものだけ」に限定した**（運営者の判断）。**対象外のものの記載は書類から削除済み。次のセッションも範囲を広げないこと。**④**この `変更履歴.md` に残っていた個人アカウント名を伏せ字にした**（§9「GitHub 側に持ち出さない」に反していたため）。⚠ **git の履歴には残ります。**完全に消すには履歴の書き換えが要るので、**やるかどうかは運営者の判断**です | （このコミット） | 済 |
| 2026-08-21 | **退任に向けた名義の棚卸しを実物で確認した（コード変更なし）。結論: コンソールサイトは退任するアカウントに依存していません。**①GCP プロジェクトの IAM は**2アカウントの共同オーナー**で、8/12 の引き継ぎ作業が生きていた。サービスアカウントはプロジェクトに属するので人が抜けても生き残る。②同プロジェクトの認証情報に **OAuth 2.0 クライアントは0件**（API キー1本とサービスアカウント1つだけ）。③退任するアカウントのリンク済みアプリで **YouTube 権限を持つのは配信ソフト2本だけ**で、Analytics 系の同意は無い。**したがって `REFRESH_TOKEN` は退任で止まりません**（旧記述の「おそらく」を実測に差し替えた）。④`fixArchiveValidation` が効いていることを確認（動画アーカイブの企画タイプの入力規則が **4本→1本**にまとまった。**手動実行はしていない**ので認可は増えていない）。⚠ **訂正: 掲出日の入力規則は1本ではなく2本に分断しています**（「ルールを追加」で足したため。どちらも「有効な日付」なので実害なし）。⚠ **退任するアカウント名義の残りは Apps Script 2件だけ**で、どちらもサイトの依存先ではない。詳細は引き継ぎ書 §6・§10 と「★ 名義で残っているもの」 | （このコミット） | 済 |
| 2026-08-18 | **スプレッドシート側だけの変更（コード変更なし）。**①投稿管理の投稿予定日（A列）に「有効な日付」の入力規則を追加（**警告を表示**。拒否ではない）。**原因はスクリプトではなく「空のセルにはカレンダーが出ない」**ことだったので、シート側で直した。②動画アーカイブの掲出日（I列）にも同じ規則を追加。③投稿計画表のバウンドスクリプトに **`fixArchiveValidation` を新設**し、`syncArchiveFromData` の末尾から呼ぶようにした（**E・F・I 列の入力規則を毎回いちばん下まで貼り直す**。企画タイプの規則が4本に分断していた原因は「新しい行にだけ貼る」実装だった）。⚠ **これを消すとまた分断します。**④**運営ログのスプレッドシートにバウンドスクリプトを新設**（`sortHomework` / `onEdit` / `onOpen`。宿題タブを 未着手 → 進行中 → 保留 → 完了 → 見送り の順、同じ状態の中は期限の早い順に並べ替える）。⚠ **シートと画面で並び順はわざと違います**（画面は `build_ops_json.py` の `STATUS_ORDER`。運営者が「サイトは今のままでOK」と決めたので揃えていません。**バグと思って揃えないこと**）。⚠ **`onEdit` は同じ値を打ち直しても発火しません。**「動いていない」と誤診しないこと。⚠ **`onEdit` / `onOpen` は simple trigger なのでトリガー一覧に出ません。**止めるにはファイルごと削除します。詳細は引き継ぎ書 §6 | （このコミット） | 済 |
| 2026-08-17 | **引き継ぎ書の「いま進行中・次にやること」を 8/14 時点 → 8/17 時点に更新**（コード変更なし）。チャットが圧縮されたため、この日の作業をすべて書類側に移した。**①「日付が決まっているもの」の表を新設**（8/18 `relogin` 判定・8/24 8/15〜8/21週の確定・**8/31 工藤さん退任**・9/01 `REPOKER01` 期限）。**②やることを4件→5件に**（8/8週の分析は完了したので削除。代わりに **`動画アーカイブ H・I列が292本中0本`** と **`8/31 に向けた退任アカウント名義の棚卸し`** を追加）。**③★「着手しないこと」の節を新設。**「レポートをもっと良くするには」への10案のうち**選ばれたのは ⑥⑦ の2件だけ**で、残り8件（①目標 ②企画タイプ別 ③アクションを前に ④先週の提案の追跡 ⑤冒頭の見出し化 ⑧1ページ化 ⑨トップ動画の行数 ⑩流入経路を構成比に）は**やらないと決定**。⚠ **消さずに残しています。同じ提案を毎回されるのを防ぐためです。**⚠ **①「目標は入れない」はいちばん強い否定**なので、KPI・達成率の類は提案しないこと。あわせて「投稿計画をレポートに戻す」「未確認を書き換える」も同じ扱いにした。**④済んだ6件を記録**（8/8週の分析・レポート全面改訂・動画別流入経路・クーポン掲出日の記録先・`REPOKER02` 廃止・`check_report_format.py`） | （このコミット） | 済 |
| 2026-08-17 | **★ グラフを2つ改善した**（オーナーの「この資料が65点だとしたときもっとどう良く出来ますか」への回答のうち、**選ばれた2件だけ**を実装。**目標の設定は「入れない」と決定**）。**⑥ 26週グラフに「山の理由」を1つだけ注記。**`fetch.py` の `build_weekly_trend()` に **`driver`**（その週の視聴回数の**3割以上を1本で稼いだとき**だけ入る `{title, share}`）を追加し、`report.html` の `trendBlock()` が**引き出し線つきで1件だけ**書くようにした。**これまでは山があっても『なぜ高いのか』が図から分からず、読み手が本文を探しに行く必要があった。**該当は26週で2週（2026-03-07 が33%、2026-08-01 が**49%**＝【神フォールド】芸人アントニー）。⚠ **注記は必ず1つに保つこと。**複数打つとグラフが読めなくなります（Few「注意を要するものだけを目立たせる」）。⚠ **見ている週と同じなら出しません**（当週は既に濃色＋直接ラベルで示されており、二重になるため）。**⑦ 基本指標の表に「この6か月」列（スパークライン）を追加。**週間視聴回数と登録者純増の**26週ぶんの形**を表の1セルに入れた（Tufte がこの用途で作った形）。**節を1つも増やさずに文脈を足すため**にこの形にしています。⚠ **見ている週までで切っています**（`TREND.slice(0, tIdx+1)`）。未来の週まで描くと「いま見ている週がどこか」が分からなくなります。⚠ **軸も目盛りも付けません。**形だけを見るもので、数値は同じ行の左側にあります。⚠ 末尾の点は当週色 `#8a6d1a`。負の値をまたぐときだけ 0 の水平線を薄く引きます | aca41db | 済 |
| 2026-08-17 | **★ レポートを「見やすさ」の原則に沿って全面的に作り直した**（オーナー「見にくい。この資料の全てにおいて。まず人間が直感的に見やすい資料作成およびチャートの作成方法を調べて徹底的にその上でまとめて」）。**思いつきで直さず、出典のある原則を調べてから**直しました（Stephen Few / NN/g / Matthew Ström / APA / A List Apart / Butterick / Typotheque / デジタル庁 / Cleveland-McGill）。**照らしたら違反が7つ**ありました: ①**表が全マス罫線**（APA「表に縦罫線は存在しない。データ行を分ける横罫も使わない」）②**ヘッダーに地色**（A List Apart「地色・背景はすべて避けよ」）③**文字サイズが6段階**（NN/g「サイズは3つまで。全部目立てば何も目立たない」）④**見出しが内容の無い句**（「基本指標」「流入経路」= NN/g の禁止例そのもの）⑤**節が8つ**（チャンクは3〜7、安全側で5以下）⑥**列間18px**（推奨は左右16pxずつ。この余白があるから罫線が要らない）⑦**本文13px≒9.75pt**（印刷は10〜12pt）。**直した内容:** 表の罫線を全部外してヘッダー下の1本だけに／地色を撤去／文字サイズを **5段階**（23/19/15/14/11.5）に／**見出しを結論の文に**（データから生成。「増減の内訳」ではなく「動いたぶんの78%は◯◯1本です」）／節を**7つ**に整理／列間を16pxに／本文を **14px（≒10.5pt）**・行間1.75に／**等幅数字を全体に**適用／囲み枠は**結論の1か所だけ**に（Few「注意を要するものだけを目立たせる」）。⚠ **ここを『見た目の好み』で戻さないこと。**1つずつ出典があり、CSSの先頭にも要点を書いてあります。⚠ **見出しを「基本指標」のようなトピック句に戻さないこと**（最初の2語で要旨が伝わるように、が原則）。結果: 総文字数 2,735字・節7・表4・グラフ4 | （このコミット） | 済 |
| 2026-08-17 | **★ レポートの設計方針を「データはチャート・文章は考察」に変えた**（オーナーの指示「まとめること以外のデータの文章を減らし、その分をチャートで直感的に見せる。まとめや分析の考察には文章を使う」）。**きっかけ: 文章と表が完全に重複していました。**流入経路の段落は「ブラウジング機能が59.9%減…」と書いていたのに、すぐ下の表に同じ数字が載っていました。新作もクーポンも同じ構造でした。**チャートを3つ新設**（`report.html`）: ①**要因分解**（`causeBlock()`／`divSVG()`。「増減はどの動画で起きたか」。**これまで文章にしかなかった『減少の78%は1本』が初めて図になった**）②**新作の初速**（`firstBlock()`。公開2日間。`fetch.py` の `new_videos` に **`first2d`** を追加。⚠ **2日そろっていない動画は `None`** にして出しません。1日ぶんを2日ぶんと並べると初速が低く見えるため）③**流入経路を表から横棒グラフへ**（今週/前週の2系列）。**AI分析の本文は考察だけに**（730→**378字**・5桁以上の数字は**ゼロ**）。**`headline` の役割も分けた**: 上部の結論バナーが**水準**（26週平均の何%か）を自動で出すので、`headline` は**読み**を書く。同じことを2回書かない。⚠ **これに合わせて §7 の本文の下限を 400字 → 250字に下げました**（`check_report_format.py` も）。数字がチャートへ移ったぶん、本文は短くなるのが正しい。**上限は動かしていません。**⚠ 配色はすべて検証済み（増 `#1f6f9c` / 減 `#c0553f` は protan でも ΔE 14.3、今週 `#8a6d1a` / 前週 `#6f8f9e` は ΔE 15.5、いずれも白地に対して 3:1 以上）。**目分量で色を足さないこと。**⚠ **ラベルの文字数は幅から計算しています**（`fitChars()`）。決め打ちにすると長いタイトルで左にはみ出して切れます（実際に -48px はみ出した）。結果: 総文字数 2,967字・グラフ 1→**4**・表 7→4・5桁以上の生数字 1個 | （このコミット） | 済 |
| 2026-08-17 | **★ 週次レポートを読みやすく作り直した**（オーナーの指摘「直感的にわかりにくい。文字が多いうえに数字が理解しにくい」）。**数えたら指摘のとおりでした: 総文字数 3,324字・節8つ・表7つ・5桁以上の『生の数字』が54個。**直したのは4点。**A 万表記**（`fmtMan()` を新設。587,400回 → **58.7万回**。⚠ **ツールチップと小さい数字には生の値を残す**。⚠ ダッシュボードの `fmt()` と同じ規則なので片方だけ変えないこと）**B 結論を先頭に1行**（`verdictBlock()`。「今週は平年並み。26週平均の106%（26週中10位）」。⚠ **判定は26週平均に対する比だけで決めています。前週比では決めません**——前週が突出していると『半減』に見えるため。実際 8/08週がそうでした）**C 表に横棒**（`.minibar`。トップ動画と流入経路。数字が右揃えなので棒も右から伸ばして右端をそろえた）**D 節を8→6**（視聴者層の2行表は基本指標の1行に畳んだ。**投稿計画は問題が無い週は節にせず1行**にし、遅れ・未投稿があるときだけその行だけ表で出す `planBlock()`）。あわせて**AI分析の本文の数字も万表記に統一**（本文は 753→730字）。結果: **総文字数 3,324→2,980字、5桁以上の生数字 54個→1個、節 8→6、横棒18本。**⚠ 残った1個は「週末時点の登録者数 10,100人」で、これは**万に丸めると意味が壊れる**ので生のままにしています | （このコミット） | 済 |
| 2026-08-17 | **★ 週次レポートに「直近26週（約6か月）の中での位置」グラフを追加**（オーナーの依頼「これ週間しかないので直近3〜6月とも比較するチャートが欲しい」）。**PDF とダッシュボードの両方**に入れた。`fetch.py` に `build_weekly_trend()` を新設して `data.json` の **`weekly_trend`**（土〜金・26週・視聴回数と登録純増）を作り、`report.html` は**外部ライブラリを使わず SVG を直接**描画、`index.html` は Chart.js で描画。**★ これが効く理由:** 2026-08-08 週は前週比 **-51.9%** ですが、**26週平均の 106%** で平年並みかやや上でした。前週が26週で突出していただけで、**前週比だけ見ると読み違えます。**⚠ `analytics_daily` は **2022-09-16 から1,429日ぶん**あるので期間の制約はありません（流入経路と視聴者層だけは週次レポート由来で 2026-05-02 以降のみ）。⚠ **7日そろった週だけ**を並べています（日数の違う週を並べると高さの比較が壊れるため）。⚠ **`report.html` を Chart.js に置き換えないこと。**CDN が落ちると印刷が白紙になります。⚠ 色は「強調」の形（見ている週だけ濃い色・他は薄いグレー・1系列なので凡例なし）。PDF は白地に対して薄いグレーが 3:1 未満なので、**目盛りと当週の直接ラベルが説明の代わり**です。外さないこと。⚠ **同じグラフが2か所にあります。**数字の元はどちらも `weekly_trend` なので、直すときは描き方だけを両方直すこと | （このコミット） | 済 |
| 2026-08-17 | **`index.html` の週セレクタが最初から効いていなかったのを修正**（グラフ追加中に発見）。週次レポートタブで**週を変えてもサマリーごと何も変わらない**状態でした。原因は `renderReport()` が `sel.innerHTML` を作り直してから `sel.value` を読んでいたこと。**作り直すと select の値が先頭に戻る**ので、いま選んだ週が消え、古い `wrSelected` に戻っていました。**`innerHTML` を作り直す前に選択値を読む**ように直し、5/09週・7/04週・8/08週で切り替わることを実機で確認。⚠ **この順番を戻さないこと**（コメントで理由を残してあります） | （このコミット） | 済 |
| 2026-08-17 | **動画アーカイブに `クーポンコード`（H列）と `掲出日`（I列）を追加**（スプレッドシートの変更・運営者の承認済み）。**★ バウンドスクリプトの改造は不要でした。**`syncArchiveFromData` は `getRange(2,1,n,6)` と `getRange(2,7,n,1)` で**列1〜7しか書かない**ので、H・I列は上書きされません（企画タイプの `TYPE_OPTS` で起きた「1時間後に静かに戻る」罠はここには無い）。ヘッダーの書式は G列からコピーして揃えた。**即時更新を走らせて実際に読めることを確認済み**（`video_archive` 292本・`coupon` は0本＝まだ誰も入れていないので正常。ログに「クーポンコードあり 0 本」と出る）。⚠ **プルダウンは付けていません。**`REPOKER04`・`05` と毎週増えるため、選択肢を直書きすると `TYPE_OPTS` と同じ罠になります。自由入力にして、必要なら `coupon.codes_read` と突き合わせて誤記を見つける方針。プルダウンが要るなら、別タブに選択肢の範囲を作って「範囲を指定したリスト」にすること（コード追加がセル1つで済む） | （このコミット） | 済 |
| 2026-08-17 | **★ 確定した週を作り直すと数字がわずかに動くことが分かった（実測 0.2%）。**`video_traffic` の実機確認のため 8/08週の `final` を一時的に外して作り直したところ、**視聴回数が 588,654 → 587,400（-1,254回・0.2%）、総再生時間も 268,909 → 268,834 に動いた。**YouTube は確定後も日次を微修正しており、**`final` は「もう変わらない」ではなく「その時点の確定値で固定した」**という意味。ふだんは確定週を作り直さないので見えない。**公開済みの分析と食い違ったので、本文・見出し・提案の数字を全部書き直した**（土台も 22.3%→22.5%、1日平均 69,367→69,188）。⚠ **`final` を手で外すときは、分析の数字を直すところまでを1つの作業にすること。**運用手順 §3 に追記済み。なお **`video_traffic` は実機で動きました**（上位5本ぶん取得。牽引動画はブラウジング機能90.0%、8/10公開分はショートフィード64.5%と、**動画によって経路構成がまるで違う**ことが初めて見えた） | （このコミット） | 済 |
| 2026-08-17 | **★ 週ごとの動画別流入経路を取得するようにした**（運営者の依頼）。`fetch.py` に `fetch_video_week_traffic()` を新設し、`update_weekly_reports` から**その週の上位5本**について `insightTrafficSourceType`＋`filters=video==VID` を取る。結果は `weekly_reports[].video_traffic` に入る。**なぜ要るか: これまでチャンネル全体の流入経路しか無く、「ショートフィードが減った」を『経路の入れ替わり』と『ショート面の露出減』に分けられなかった**（2026-08-08 週の分析で実際に詰まり、提案として出していた）。⚠ **既存の `fetch_video_extra_analytics` とは別物です。**あちらは**通算365日**の動画別で、週の比較には使えません。**混ぜないこと。**⚠ **すでに確定している週には入りません**（確定週は作り直さないため）。この日以降に確定する週から溜まります。⚠ **上位5本ぶんだけ**なので、5本で全体を代表させないこと。1本1リクエストで、確定していない週でしか動かないため実質1週1回。取れなかった動画はキーごと入らず、全部失敗しても前回値を落とさない（レポート生成を止めない） | （このコミット） | 済 |
| 2026-08-17 | **動画アーカイブの「クーポンコード」「掲出日」列を読めるようにした**（運営者の質問「掲出日はどのように記録すればいいか」への回答）。`fetch_video_archive()` を列名で探す方式に整理し、`video_archive` に `coupon` / `coupon_date` を入れる。**⚠ シートにこの2列がまだ無くても動きます**（空になるだけ。実行ログに「クーポンコード列はまだありません」と出る）。**掲出日は旧作に後から貼ったときだけ記入**（新作は公開日と同じなので空でよい）。⚠ **列は名前で探しています。**足しても並べ替えても壊れませんが、**列名を変えると黙って空になります**（エラーになりません）。運営ログの施策シートに書く案は、動画との紐付けが手作業になるため採らなかった。**列の追加はまだです。追加されるまで `coupon` は空のままです** | （このコミット） | 済 |
| 2026-08-17 | **`REPOKER02` を「未使用の想定」から「廃止」に確定**（運営者に確認・コード変更なし）。**どこにも掲出していない**ことが確認できたので、8/10 から残っていた「もしどこかに掲出済みなら要修正」は解消。入力期限の記載も外した（掲出していないので意味がない）。⚠ **`coupon.codes_read` に `REPOKER02` が出てこないのは正常です。欠落と読まないこと。**あわせて**「インサート映像のテンプレ化」は取りやめ**（運営者の判断）。運用手順 §2-4 の実例は**4点の埋め方を示す例としてだけ**残し、進行中の施策として扱わない旨を明記した。⚠ **運営ログの施策シートの行は消していません。**サービスアカウントは閲覧者・スコープも `readonly` で、**セッションはスプレッドシートに書き込めない設計**です（運用手順 §6）。行の削除は運営者の操作が要ります | （このコミット） | 済 |
| 2026-08-17 | **完了報告を「常に md のコードブロック」から「普段は普通の文章」に戻した**（運営者の判断）。8/14 に固定したときの理由は「運営者がそのままコンソールのメインチャットへ貼るため」でしたが、**実際には貼らずに自分で読んでいた**ため、①書式が効かない ②見出し6つ固定で中身の無い節にも「なし」と書く ③結果的に長い、の3つが損になっていました。**「貼る用にください」と言われたときだけ**コードブロック1つにします（そのときの守ることと雛形は残してあります）。★ **これは同じ日に新設した「書類を増やすときの決まり」の最初の適用です。**3日前に理由があって足したルールが、**理由のほうが変わったのに残っていた**ケースでした。直したのは3か所（`CLAUDE.md` §2 / 引き継ぎ書の★の章 / 運用手順 §9）。**3つは同じ内容に保つこと**は変わりません。引き継ぎ書 118,111→118,182B、運用手順 49,589→**49,499B** | （このコミット） | 済 |
| 2026-08-17 | **引き継ぎ書から決着済みの記述を削除し、「書類を増やすときの決まり」を新設**（運営者の判断・コード変更なし）。**⚠ Dropbox に git 履歴は無いので、消したものをここに残します。**削除したのは6件・計3,867B: ①冒頭の「2026-08-10 更新」メモ（内容は本文に反映済み）②★の章の「一度 `docs/` へ移して取りやめた」2行（**「リポジトリに残す md は2つだけ」は残した**）③いま進行中の完了項目「`docs/` から同期する→消滅」1行 ④ライブ配信ボットの「⚠ 日付の扱いを一度見てください」（**8/14 に確認済みなのに、やるべき仕事として残っていた**。確認済みの結論と「将来このボットに日付処理を足すときは `Utilities.formatDate` を使う」に置き換えた）⑤「宿題: 運営ログの自動反映」の やりたいこと＋完了バナー（見出しも「運営ログの自動反映（2026-08-12 から稼働中）」に改名）⑥同じ節の「アカウント消滅リスクは解消」ブロック（**3か所に重複していたうちの最長**。§7 と「いま進行中」の2か所は残した）。**★ 私の見積もりが間違っていた件も記録:** 当初「A1〜A6 で約12KB 消せる」と報告したが、**見出しだけを見て中身を読んでいなかった。**実際に読むと A1 の 7,247B のうち消せたのは 2,120B だけで、残りは **GCPプロジェクトID・サービスアカウント名・スコープ・Secret名・動作確認の手順**という**いまの設定そのもの**だった。安全に消せたのは全体の 3.2% のみ。この教訓を★の章に「削る前に、見出しではなく中身を読む」として書いた。あわせて新設した決まりは①事故の記録に経緯を書かない ②1つ足したら1つ畳む ③削る前に中身を読む ④消したら `変更履歴.md` に書く、の4つ＋**消してはいけないもの**（「見送りました・提案しないでください」「復活させないでください」「`_廃止_` は消さないこと」と §10 の取り消し線＝「調べ済み」の印）。引き継ぎ書 119,479 → **118,111B**、運用手順は増やし方のポインタのみ追加で 49,165 → 49,589B | （このコミット） | 済 |
| 2026-08-17 | **★ 週次AI分析の形を機械が見張るようにした（`check_report_format.py` を新規追加）。**膨張の根本原因は「**このプロジェクトで唯一、自動チェックが無かった場所だった**」ことです。書類の更新漏れ・データの鮮度・シートが読めない・未入力は全部 Actions と LINE が見張っているのに、**分析の中身だけ誰も数えていませんでした。**そのため運用手順 §7 の「本文 400〜700字程度」を3.7倍まで超過しても公開まで気づけませんでした。**7月まで7週連続で守れていた実績があっても、外れたことに人は気づけません。**見るのは①字数（350〜760字で警告。§7 の「程度」に上下1割弱の幅）②`headline` の有無 ③`suggestions` が `{title, actions}` か ④本文に ⚠ が混ざっていないか ⑤冒頭に確定状況の一文が無いか、の5つ。`daily_fetch.yml` から **JST 9時だけ**通知する（`docs_check` と同じ。LINE の通数を増やさないため）。⚠ **警告の幅（350〜760）を広げないこと。**広げるほど「守れていないのに気づけない」状態に戻ります。⚠ **この検査は「形」しか見ません。中身が正しいかは人が読むしかありません** | （このコミット） | 済 |
| 2026-08-17 | **過去の週次AI分析9週すべてが §7 に違反していたので直した。**新しい検査にかけたところ、**分析のある9週すべてがどこかに違反**していました（1つも合っていないのに通知は1度も出ていない）。①**6週（6/13・6/20・7/04・7/11・7/18・7/25）に `headline` が無かった** — §7 は「必須。ダッシュボード最上部のバナーに出る」と決めており、**PDFの結論欄とバナーが空のまま**でした。**その週の本文に書いてある数字だけ**から見出しを作って補った（新しい分析はしていない）②同じ6週の `suggestions` が文字列の配列だったので `{title, actions}` に**機械的に包んだ**。**文字はひとつも変えていない**（`report.html` は元から両方を同じ見た目で描くので表示は変わらない）③**8/01 週の本文を 1,166字 → 694字に短縮。**この週が「正しい見本」として参照され、8/08 の 2,586字を招いた引き金です。落としたのは手法の説明と注記だけで、**結論と「牽引1本を除いた土台」は残した。**冒頭の「7日分すべて確定した確定値。」も削除。検査は 9/9 週で緑になった。⚠ **既存の見出し・他の週の本文・提案の文字列は1文字も触っていない**（機械で突き合わせて確認済み） | （このコミット） | 済 |
| 2026-08-17 | **週次レポートを「必要な情報だけ」に絞った（オーナーからの指示）。**本文 2,586字 → **753字**、PDF 5ページ → **3ページ**、提案4件 → 3件。**運用手順 §7 はもともと「本文 400〜700字程度」と決めており、3〜6倍に超過していたのを戻した形**（前週 8/01 も1,300字あり、じわじわ伸びていた）。落としたもの: σ・検出限界の数値、分母の内訳、動画別合計99.7%の検算、⚠ の注記すべて。**残したもの: 「牽引1本を除いた土台 -22.3%」。**これが無いと -51.8% が「チャンネルが半減した」と読まれ、**数字の意味が正反対になる**ため（減少の78%は7/25公開の1本の減衰）。あわせて **`report.html` のクーポン節から ⚠ の注記3行と日数違いの警告を削除**（オーナーの判断でレポートには注記を出さないことにしたため）。**日数は表のセルに「28件（7日・1日4.00件）」の形で残るので情報は失われない。**⚠ **落とした注記の中身は運用手順 §3 の①〜⑧に全部残っています。**レポートに出ないだけで、分析する側は必ずそちらを読むこと。書き込みスクリプトに**本文の字数と ⚠ の混入を止める assert** を入れた（実際に924字で1回弾かれた） | （このコミット） | 済 |
| 2026-08-17 | **8/08〜8/14 週の分析から3点を削除**（運営者の指示・`data.json` のみ）。①本文冒頭の「7日分すべて確定した確定値。」 — レポート上部に「確定 7/7日」のバッジが出ているので重複 ②**運営ログの施策3件の評価段落**（ショート字幕／回遊構成の最適化／インサート映像のテンプレ化） — 3件とも「判定不能」で、オーナー向けの本文では情報量にならない ③提案「施策を判定できるようにする記録を先に足す」。本文6段落・2,586字、提案4件になった。⚠ **運用手順 §7 の「施策の評価を入れる」は、残したクーポン施策の段落（§2-4 の4点で評価）で満たしています。**⚠ **削除した2点に入っていた申し送り2件は、レポートには載らなくなりました**（「インサート映像のテンプレ化」の開始日が空欄／ショート字幕の有無を動画単位で持っていない）。**運営ログの施策シートにしか残っていないので、必要なら別の形で追うこと** | （このコミット） | 済 |
| 2026-08-17 | **`report.html` に「クーポン施策」の節を新設**（運営者の依頼「新しく始まったクーポンによる施策もレポートに追加してほしい」）。今週／前週の入力件数・使用率・実人数、コード別（通算）の入力・使用・使用率・実人数、入力から使用までの時間を表で出す。⚠ **件数だけを週どうしで並べない。**`coupon.daily` から「その週に入力のあった日数」を数えて **1日あたりを必ず併記**し、日数が違う週どうしのときは表の下に警告を出すようにした（**クーポンの入力データは 2026-08-04 が最初**で、8/01週は4日ぶん・8/08週は7日ぶんしかない）。クーポンがまだ無い週は節ごと出さない（7/11週で確認済み）。注記として①1件＝1回の入力で1人ではない ②取得日の直前の入力は判定時間が足りず使用率が低めに出る ③掲出日の記録が無いので動画ごとの効果測定ではない、の3点を常時表示 | （このコミット） | 済 |
| 2026-08-17 | **★ 引き継ぎ書に書いたばかりの「確定時に古い分析が固定される」が、その日のうちに実際に起きた。**8/08〜8/14 週を6日分で書いた直後（`dbe0506`）に 8/14 の確定値が入り、自動更新（`1d1668c`）が **`final:true` を立てながら6日分の `ai_analysis` をそのまま引き継いだ**。`final` になった週は二度と作り直されず、`ai_analysis` があるので「分析待ち」バナーにも出ない。**push が非 fast-forward で弾かれて気づけた**（弾かれなければ、6日分の分析が確定週に貼りついたまま残っていた）。**7日分で全面的に書き直した。**主な数字の変化: 視聴回数 520,109→**588,654**（前週比 -53.8%→**-51.8%**）、総再生時間 238,143→**268,909分**、牽引1本を除いた土台 -24.4%→**-22.3%**（1日平均 70,195→**69,367回**）、ショートフィード -27.7%→**-28.1%**、関連動画 -27.1%→**-26.1%**。⚠ **確定前に書いた分析は、確定した瞬間に固定される。**次からは §5 のとおり確定を待つこと。急ぐときは `変更履歴.md` に `未` で積み、**push が通ったかどうかではなく `final` を直接見て**書き直すこと。あわせて本文冒頭の断り書きを運営者の指示で短縮したが、**確定したので「まだ確定していません」ではなく「7日分すべて確定した確定値」に直してある** | （このコミット） | 済 |
| 2026-08-17 | **クーポン比較の日数違いを訂正し、クーポンを施策として評価し直した。**6日分の版では「当週28件 vs 前週35件」と並べたが、**前週の35件は 8/04〜8/07 の4日ぶん**、当週の28件は7日ぶんで、**7日 対 4日**の比較になっていた（運用手順 §3 の「日数の違う週どうしを引き算しない」と同じ誤り）。1日あたり **4.00件 対 8.75件** に直し、**前週は発行直後の山なので定常状態とは比べられない**ことを明記。§2-4 の4点（機序／測れるか／必要な期間／いま言えること）で評価し直し、**REPOKER03 はこの週が初出で17件すべてがこの週**、REPOKER01 はこの週11件で合計28件、という内訳を追加。使用率は REPOKER01 78.3%(36/46)・REPOKER03 70.6%(12/17) だが、**新しいコードほど右側打ち切りの影響を受ける**。⚠ **8/14 の6件中1件しか使用済みでないのは、シート取得日が 8/15 で判定時間が足りないため**で、使われなかったのではない。提案に「クーポンの掲出日を記録して動画ごとの効果を測れるようにする」を追加。**掲出日の記録が無いので、動画への効果の答えは「判定できない」のまま** | （このコミット） | 済 |
| 2026-08-17 | **`report.html` の印刷CSSを直した**（PDF にツールバーが写り込んでいた）。`@media print` の `.toolbar{display:none}` が効かず、「← ダッシュボード」「週の選択」「印刷/PDF保存」がPDF の1ページ目に入っていた。**原因は JS が `toolbar.style.display='flex'` をインラインで当てているため**で、インラインスタイルのほうが優先される。`display:none!important` に変更。⚠ **`!important` を消さないこと**（消すと同じことが起きます）。理由をCSSにコメントで残した。実機で印刷メディアの可視／不可視を両方確認済み | `7760807` | 済 |
| 2026-08-17 | **8/08〜8/14 週の週次AI分析を書いた。⚠ この週は `final:false`（確定6/7日・8/14待ち）で、運用手順 §5 の「`final` が false のレポートには絶対に書き込まない」に反する。2026-08-17 に運営者が明示で「確定を待たず6日分で差し替える」と判断した例外です。**確定して7日分になったら書き直すこと（`fetch.py` は `final` になるとき既存の `ai_analysis` をそのまま引き継いで固定するので、放っておくと6日分の分析が確定週に貼りついたまま残ります。しかも `ai_analysis` があるので「分析待ち」バナーにも出ません）。本文冒頭に「まだ確定していない・確定後に書き直すこと」を明記済み。あわせて**それ以前の 8/15 付の分析（確定5日ぶん・`headline` も `prompt_version` も無し・`suggestions` が文字列配列）を、§7 の形（`headline`／`{title,actions}`）に置き換えた**。⚠ **オーナー送付用レポート（`report.html`）は、確定していない週では「今週6日ぶん ÷ 前週7日ぶん」の増減率を出します。**総再生時間・流入経路・視聴者層の3か所が該当（基本指標の週間視聴回数と登録者純増は両週とも7日ぶんのスナップショットなので正しい）。`report.html` の `partial` フラグは `views_snap` があると false になるため、「確定N日分のみ」の注意書きも出ません。**この週のPDFを送るなら、その3か所は読み飛ばすよう添えること。**直すかどうかは運営の判断（§3 の「`confirmed_days` が違う週どうしを引き算しない」に当たります） | `dbe0506` | 済 |
| 2026-08-14 | **⚠ 引き継ぎ書に未反映の節が1つあります（`08b79a5` の取りこぼし）。**ライブ通知ボットの日付確認を反映したセッションでは、**§9 に「★ 秘密の値が埋まっているソースを調べるときのやり方」を新設**していたが、**デバイスブリッジが切れて Dropbox に書き戻せず**、別パソコンの引き継ぎでも**添付が届かなかった**ため、この節だけが現物に入っていない。内容は「Apps Script のコードにはトークンとAPIキーが直接書かれているので、調査でソースを読むだけでも中身が流出しうる。**中身を取り出さず、パターンの出現回数だけを数える**（全部0回なら扱っていないと確定できる）。**数えた結果・行数・関数名は書いてよいが、ソースを丸ごと貼らない**」。**次に Dropbox に届くセッションが §9 のセキュリティ制約の箇条書きの直後に足すこと** | （このコミット） | 済 |
| 2026-08-14 | **「完成版で上書きする」形の引き継ぎが静かなロールバックを起こしかけた（事故・コード変更なし）。**あるセッションが完成版を作ったがブリッジが切れて書き戻せず、別パソコンの新しいセッションに「添付を上書きして」と渡した。**添付が届いておらず、手元にあったのは2日前の旧版だった**（55,858 バイト 対 現物 106,455 バイト）。**そのまま上書きしていたら2日分の約50KB が消えていた。**書き戻す前にサイズと中身を現物と突き合わせたことで気づけた。**引き継ぎ書の★の章に「書き戻す前に、必ず現物と突き合わせること」を新設**し、①上書き前に現物を読んで比べる ②`expectedMtimeMs`（mtime ガード）を付ける ③書き戻したあと読み直して検証する ④迷ったら全体上書きではなく**差分だけ足す** ⑤**添付を前提にした引き継ぎをしない**（渡す側は何を直したかを `変更履歴.md` に書く）の5つを明記する。**次に Dropbox に届くセッションが反映すること** | （このコミット） | 済 |
| 2026-08-14 | **ライブ通知ボットに「`new Date()` の癖」は無いことを確認（宿題 #5 の解消・コード変更なし）。**旧プロジェクト（**退任するアカウント**所有・移管先のコピー元）のソースを機械的に数えたところ、**`new Date(` 0回 / `.getFullYear`・`.getMonth`・`.getDate`・`.getHours`・`.getMinutes`・`.getDay` 0回 / `Utilities.formatDate` 0回 / `Date.now(` 0回 / `toISOString` 0回 / `publishedAt`・`scheduledStartTime` 0回**。**日付・時刻を扱うコードが1行もありません**（通知済みの動画IDを `PropertiesService` に覚えているだけ。関数は `checkLives` `matchKeyword` `getNotified` `saveNotified` `pushToLine` `doPost` `showCapturedGroupId` `testSetup` の8つ・全144行）。したがってバウンドスクリプトで起きたタイムゾーンの癖は**このボットには影響しません**。⚠ **見たのはコピー元の旧プロジェクトです。**8/14 のコピー後に `ytpokerguild` 側でコードを直していれば、その分は未確認。⚠ ソースにはトークンとAPIキーが含まれるため、**中身は取り出さず、パターンの出現回数だけを数えました** | （このコミット） | 済 |
| 2026-08-14 | **運営者の判断3件を反映**（スプレッドシートとバウンドスクリプトの変更・**リポジトリのコードは変えていません**）。①**配信元の表記を9択に統一。**`Games`（1件）→ `JOPT`、`JOPT本配信`（1件）→ `JOPT`。⚠ **検索と置換で「完全に一致するセルを検索」を入れ、範囲を `'投稿管理'!F2:F745` に限定して**実行した（**範囲を切らないと E列の `JOPT Games` まで巻き添えになる**）。これで F列は9択だけになった ②**ステータス（D列）の入力規則を `D2:D48` → `D2:D747` に拡張。**48行目までしか無く、49行目以降でプルダウンが出なかった。⚠ **既存ルールの「範囲に適用」を書き換えたので、ルールは1つのまま**（新しいルールを足すと範囲が分断され、動画アーカイブの企画タイプで起きたのと同じ状態になる） ③**A2 の日付を「過去の日付のときだけ」入れ直すようにした。**`onSelectionChange` に、**A2 が過去の日付かつ行2の他の列が全部空**のときだけ今日に入れ直す条件を追加。**先の日付（8/20 など）を意図的に置いた場合と、書きかけの行は触りません。**実機で 8/1 → 8/14 に入れ直ることを確認（テストで増えた行は削除済み） | （このコミット） | 済 |
| 2026-08-14 | **Apps Script（ライブ通知ボット）の `ytpokerguild` への作り直しは 2026-08-14 に完了済み**（記録が残っておらず「完了したか未確認」のままだったので追記・コード変更なし）。`ytpokerguild` 所有で新しいプロジェクトを作り、`checkLives` の**5分おきトリガーを設定して実行ログで成功を確認**（運営者が承認し、実行開始／実行完了のログを確認済み）。**旧プロジェクトのトリガーは削除済み（0件）。**これで「アカウント消滅リスク」の最後の1件は解消。⚠ **このプロジェクトは**移管先のアカウント**所有なので**退任するアカウント**のブラウザからは開けません**（`script.google.com/accounts?authuser=2` に飛ばされて失敗する。2026-08-14 に実際に確認しようとして開けなかった）。**今後の稼働確認は `ytpokerguild` でログインして実行ログを見ること。**⚠ 同じ日にバウンドスクリプトで **`new Date()` のローカル系メソッドが日本時間にならない**問題が出ているので、**このボットが日付を使っているなら同じ癖が出ていないか一度確認したほうがよい** | （このコミット） | 済 |
| 2026-08-14 | **入力依頼通知まわりの記述を2点補強**（コード変更なし）。①**未入力が0本のときは朝ブリーフに「📝 未入力 N本」の行が出ない**ことを明記。**行が出ない＝全部入っている**であって機能停止ではないが、書いていないと「動いていない」と誤診される（このプロジェクトでは鮮度バナー・`ops.json` の `updated_at` と同じ型の誤診を2回起こしている）。動作を確かめる方法もあわせて記載 ②**送られる LINE の文面の実物**を引き継ぎ書に転記（文面を変えるときの基準が無かったため）。あわせて**ステータス（D列）の記述の数え方の誤りを訂正**（`D2:D48` なので「49行目より下」ではなく「48行目まで／49行目以降は出ない」が正しい） | （このコミット） | 済 |
| 2026-08-14 | **新しい動画が増えたら「企画タイプとナレーターを入れてください」と LINE で頼むようにした**（運営者の依頼「記載漏れをなくしたい」）。`notify_archive_input.py` を新規追加し、`daily_fetch.yml` の**コミットより前**に実行する。⚠ **コミット後に動かすと新作が常に0本になり、永久に何も送らなくなります**（前回の動画一覧を `git show HEAD:data.json` から読んでいるため）。本文は「新作N本 → タイトル最大5本 → 直近180日の未入力本数 → 動画アーカイブタブのリンク」。**前回の `data.json` が読めないときは何も送りません**（全部を新作と誤認しないため）。あわせて**朝ブリーフに「📝 企画タイプ／ナレーターの未入力 N本」の行を追加**（残っている限り毎朝出る。LINE の通数は増えない）。⚠ **未入力は直近180日ぶんだけ数えます**（全期間だと 2022 年のテスト動画1本がずっと残り「未入力1本」が消えない）。**同じ数え方が `notify_archive_input.py` と `morning_brief.py` の両方に書いてある**ので、変えるときは両方直すこと。⚠ 動画アーカイブの**行そのものはバウンドスクリプトが1時間おきに追加する**ので、通知のほうが先に届くことがある（本文で断っている） | （このコミット） | 済 |
| 2026-08-14 | **投稿管理の配信元（F列）をプルダウンにした**（スプレッドシートの設定変更・コード変更なし）。`F2:F745` / `JOPT` `WSOP` `WPT` `Triton` `EPT` `APT` `APPT` `Hutsler` `その他` / **入力を拒否** / 表示スタイルは**矢印**（担当G列と同じ。チップだと列幅で切れる）。選択肢は運営者の指定。⚠ **既存の値のうち `Games`（1件）と `JOPT本配信`（1件）はこの9択にありません。**「入力を拒否」は新しい入力だけを止めるので既存の値は消えませんが、**選び直すことはできません**。9択に足すか、値のほうを直すかは運営の判断。F列にはもともと入力規則が無かったので分断は起きていない。**なお ステータス（D列）だけ範囲が `D2:D48` と短く、他（E/F/G/H は 745〜747行）と揃っていません**（今回は触っていない） | （このコミット） | 済 |
| 2026-08-14 | **サンドボックスから push できないセッションがある件を書類に明記**（コード変更なし）。別セッションで、`http.extraHeader` を使った正規の方法でも `access denied by the git proxy: … is not in this session's authorized repository set` が出て push できなかった。**プロキシがそのセッションに対してこのリポジトリを許可していない**状態で、**トークンの問題ではないため入れ直しても直らない。**逃げ道は **GitHub のウェブ編集画面から commit する**こと（実際にそうして PR #2 をマージした）。同じ日に別のセッションからは通っているので、**まず `git push` を試して、駄目ならウェブ編集に切り替える**運用にした。引き継ぎ書 §2 の「push が 403 になったときの対処」に追記 | （このコミット） | 済 |
| 2026-08-14 | **投稿管理の A2（投稿予定日）を選ぶと、今日の日付が自動で入るようにした**（運営者の依頼「ダブルクリックしたら今日の日付が出るのが理想」）。バウンドスクリプトの `行の自動追加.gs` に `onSelectionChange(e)` を追加。**Apps Script にダブルクリックのイベントは無いので、選んだ（クリックした）だけで入ります。**入るのは **A2 が空のときだけ**で、すでに何か入っていたら触りません。スクリプトからの書き込みなので `onEdit` は動かず、これだけでは行は増えません（行が増えるのは人が打ち込んだときだけ）。表示形式は1つ下（A3）からコピー。**⚠ `new Date()` の `getFullYear/getMonth/getDate` が、プロジェクトの設定もスプレッドシートの設定も「日本標準時」なのに日本時間になっておらず、実測で1日前（8/13 15:00）が入った。**`Utilities.formatDate(new Date(), スプレッドシートのタイムゾーン, 'yyyy/MM/dd')` に直して解決。**このプロジェクトで日付を扱うときは JS の Date のローカル系メソッドを信用しないこと。**なお**カレンダーから選ぶ機能は元から効いていた**（日付が入っているセルをダブルクリックすると出る Google 標準の機能。入力規則は足していない）。**⚠ 残る癖：A2 に日付が入ったまま何も書かずに放置すると翌日も古い日付のまま**（「他の列が空なら今日に入れ直す」は、先の日付を意図的に置いた場合に壊すので入れていない）。**リポジトリのコードは変えていません** | （このコミット） | 済 |
| 2026-08-14 | **クーポンタブに「新規／既存の内訳」を追加**（運営者の判断で **アカウント作成 → コード入力が24時間以内＝「新規」** と定義）。`index.html` に `cpNewCard` を新設し、`signup_lag_buckets` の分布（新規側の2本を緑で色分け）と `relogin` の内訳を並べた。**しきい値は `index.html` の `NEW_LABELS` にしかありません**（`fetch.py` は分布を出すだけで線を引いていない）。**変えるときは `NEW_LABELS` と画面の説明文の「24時間」を必ず両方直すこと**（片方だけだと数字と説明が食い違ったまま誰も気づけない）。画面には**評価の前提を明記**した：①先に登録して後日コードを入れた人は「既存」に入るので**新規は少なめに出る** ②`account_oldest` に件数が集中していたらそれは**サービス側の起点**であって「既存」は上限としてしか読めない ③この表は**コードを入れた人しか載らない**ので動画の新規獲得力の測定ではない ④再ログインの「判定期間前」は「なし」ではないので**割合の分母から外している**。実測（2026-08-14・延べ59件）は新規8件13.6%／既存51件86.4%／うち即時5件、再ログインは59件すべて判定期間前 | （このコミット） | 済 |
| 2026-08-14 | **完了報告のかたちを固定した**（運営者からの明示の指示・コード変更なし）。**報告の全体を md のコードブロック1つに入れてチャットに出す。**運営者がそれをそのままコンソールのメインチャットに貼るため。ブロックを分ける／地の文をブロックの外に足すと**貼るときに内容が落ちる**（「運営者に手作業を渡さない」と同じ考え方）。見出し6つ（やったこと／変更／確認したこと／判断が要ること／次にやること／⚠注意）を固定し、**中身が無い節も「なし」と書いて残す**（書き忘れと無かったを区別するため）。**ブロックの中に3連バッククォートを入れない**（そこで閉じるため）。雛形を `CLAUDE.md` §2・引き継ぎ書の★の章・運用手順 §9 の**3か所に同じ内容で**置いた（週次AI分析版は「判定できなかったこと」を独立の節にしてある） | （このコミット） | 済 |
| 2026-08-14 | **引き継ぎ書の主要ファイル早見表にあった古い記述を1行訂正**（コード変更なし）。`fetch_ops.py` の欄が「`SHEETS_REFRESH_TOKEN` 未設定のため現在は休止」のままだったが、**2026-08-12 にサービスアカウント（`SHEETS_SA_KEY`）方式で稼働済み**で、同じ書類の §4・§10 と食い違っていた。稼働中に修正し、`SHEETS_REFRESH_TOKEN` は未設定のままでよいフォールバックである旨を明記。**上の8行を反映するついでに見つけたもので、単独の指示によるものではない** | （このコミット） | 済 |
| 2026-08-14 | **クーポンシートのタブを自動で列挙するようにした**（新しいコードのタブが増えても `COUPON_TABS` に手で足す必要がなくなった）。発行元にサービスアカウントの閲覧者共有を依頼する案（案C）を用意していたが、**シートの共有が「リンクを知っている全員＝閲覧者」のままだったため、共有を足してもらわなくても Sheets API でタブ一覧が引ける**ことが分かった（2026-08-14 確認）。`_sa_sheet_titles()` をスプレッドシートID指定に一般化し、`_coupon_tabs()` を追加。**列構成が違うタブ（発行元が作った集計タブ）は `COUPON_REQUIRED_COLS` で判定して黙って飛ばす**（`skipped_tabs` に出す）。自動列挙が効いているかは `coupon.tabs_auto` で分かる。失敗したら従来の `COUPON_TABS`（予備表）に落ちるので、消さずに残してある | （このコミット） | 済 |
| 2026-08-14 | **クーポンシートに増えた2列を取り込んだ。**①`アカウント作成日時` → コード入力までの差を `signup_lag_buckets` に**分布として**出す。**「新規／既存」の線引きはコードに埋め込まない**（どこで切るかは運営の判断）。⚠ 実測ではアカウント作成日が **2026-06-17 の夜に集中**しており（延べ59件中39件が 6/17〜6/25）、これは**サービス側の起点**であって「その日に新規登録した」とは限らない。よって「30日以上前に作成＝既存」は**上限としてしか読めない**。いちばん古い作成日を `account_oldest` に出して、この床が見えるようにした ②`コード入力14日後以降の再ログイン` → `relogin` に**値ごとの生の件数**で出す。**「判定期間前」を「なし」に丸めない**（丸めると再訪率が実際より低く出る。8/4入力ぶんの判定が出るのは 8/18 ごろで、2026-08-14 時点では全件が「判定期間前」）。**画面（`index.html`）への表示はまだ入れていない**（切り方が決まってから） | （このコミット） | 済 |
| 2026-08-14 | **投稿管理の2行目に入力すると、その上に空行が1つ自動で増えるようにした**（運営者の依頼。「使い方」タブにある「2行目を選んで Ctrl+Alt+= で上に1行挿入」という手作業を置き換えたもの）。投稿計画表に**バウンドされた Apps Script**（スプレッドシートの 拡張機能 → Apps Script）に `行の自動追加.gs` を新規作成し、`onEdit(e)` を1つ置いた。**どの列に入力しても増える**（運営者の指示）。値を消したときと、複数行にまたがる貼り付けのときは増やさない（何行増やすべきか決められないため）。**simple trigger なので承認は不要で、トリガー一覧の画面には出ません。止めたいときはこのファイルごと削除してください。**スクリプトや API からの書き込みでは動きません（simple trigger の仕様）。増えた行はプルダウン（入力規則）を引き継ぐことを実機で確認済み。**リポジトリのコードは変えていません**（`fetch.py` / `index.html` は無関係） | （このコミット） | 済 |
| 2026-08-14 | **バウンドスクリプトの `TYPE_OPTS` に企画タイプの4択が直書きされていたのを6択に直した**（`海外切り抜き` `国内切り抜き` `大会プレー` `店舗ハンド` `解説動画` `JOPT Games`）。同じスクリプトの `syncArchiveFromData` が1時間おきに動画アーカイブの入力規則を貼り直すため、**シート側で直したプルダウンが古い4択に静かに戻される**状態だった。動画アーカイブの入力規則が行ごとにバラバラだった原因もこれの可能性が高い。**企画タイプを増やすときは、シートの入力規則とこの `TYPE_OPTS` の両方を直すこと**（片方だけだと数時間後に戻る） | （このコミット） | 済 |
| 2026-08-14 | **運用ルールを2つ書類に加筆**（コード変更なし）。①**クーポンのコードのタブが増えたら、その都度 `COUPON_TABS` に足して回収する**（足し忘れると黙って抜ける。気づき方は `coupon.codes_read` を見る）。手作業をなくすため、**発行元にサービスアカウントの閲覧者共有を依頼することを決めた** ②**投稿管理の担当（G列）をプルダウン化**（`G2:G745` / `工藤` `SAW` / **入力を拒否**）。担当者が増えたら都度リストに追加が必要。あわせて**入力規則は列ごとに範囲がバラバラなので、まとめて選んで「ルールを削除」を押すと他列まで巻き添えで消える**旨を警告として明記（2026-08-14 に実際に消してしまい Ctrl+Z で復旧した） | （このコミット） | 済 |
| 2026-08-14 | **クーポンシートのタブ分割に対応し、`REPOKER03` が数字から丸ごと抜けていたのを修正。**発行元が依頼した「コード列」ではなく**コードごとのタブ**でシートを作り直したため、gid 指定なしの CSV エクスポートでは**先頭タブ（REPOKER01）しか読めていなかった**。`fetch.py` に `COUPON_TABS`（コード名, gid の表）を追加し、タブごとに読んで**タブ名をコードとして扱う**ようにした。取得日は**タブごとに**最新を見る（全タブ共通の最新日で切ると、更新が遅れたタブが丸ごと落ちるため）。`has_code_column` が True になり **`by_code`（コード別内訳）と週別の `codes` が出るようになった**。読めたタブを `codes_read`、失敗を `read_errors` に出して欠けに気づけるようにした。**⚠ 新しいコードのタブが増えたら `COUPON_TABS` に1行足すこと**（足し忘れると黙って抜ける）。暫定対応で、発行元にサービスアカウントを共有してもらえればタブを自動列挙できる | （このコミット） | 済 |
| 2026-08-14 | **鮮度バナーの文言を修正**（運営者の指示・B案）。旧文言「自動更新が停止している可能性があります」がサーバー側を疑わせる誤誘導だった。**このページは開いたときに1回しかデータを取らず**（`setInterval` も `visibilitychange` も無い）、タブを開きっぱなしにすると中身は開いた時刻のまま経過時間だけ伸びるため、翌日には必ずこの警告が出る。実際に「23時間止まっている」と誤診したが、公開ページは最新を返しており正常だった。新文言は「まず右上の『↻ 更新』を押してください。押しても変わらない場合は自動更新そのものが止まっています」 | （このコミット） | 済 |
| 2026-08-14 | **引き継ぎ書の誤りを2点訂正**（コード変更なし）。①**`ops.json` の動作確認方法が誤っていた。**旧記述は「`updated_at` が当日になっていれば動いています」だったが、`fetch_ops.py` は**中身が前回と同じなら書き換えない**設計（`updated_at` を除いて比較）。つまり `updated_at` は「最後にスプレッドシートの中身が変わった日」であって、最後に動いた日ではない。**古くても正常。**実際にこの記述のせいで「2日止まっている」と誤診した（運営ログの `modifiedTime` が 8/12 のままで、動作は正常だった）。正しい確認方法3つに差し替え ②Apps Script 移管の予定日を **8/15（金）→ 8/14（金）** に訂正。8/15 は土曜で、運営者の言った「金曜日」は 8/14 だった。**⚠ 書き換えた本文は用意できているが、このセッションの途中でデバイスブリッジが切れ Dropbox に書き戻せていない。**次にブリッジが繋がるセッションで反映すること | （このコミット） | 済 |
| 2026-08-12 | **運営タブから決定事項・施策ログの一覧を外し、議事録へのリンクに置き換えた**（運営者の判断。「このページで読むものではなく議事録そのものに当たれたほうがよい」）。`index.html` の `renderOpsMinutes()` から `決定事項`/`施策ログ` のブロックを削除し、運営ログの「議事録」タブ（`gid=1237349491`）へのリンクを表示。**`ops.json` からは消していない（表示だけ）**。週次AI分析が決定事項を読んで「決まったことを提案し直さない」判断に使うため、データごと消すと同じ提案を繰り返す。非公開件数の注記も宿題のぶんだけに修正。**議事録そのものの URL は `ops.json` に出さないこと**（実物は認証なしで誰でも読める URL のため、出すと議事録の中身が実質公開になる）。Playwright で実描画を確認済み（未完了8件・完了10件の折りたたみ・リンク先 gid） | （このコミット） | 済 |
| 2026-08-12 | **議事録のAI要約は「運営者がミーティング後にチャットで依頼したときに実行」と決めた**（定期実行はしない・週次AI分析と同じ運用）。担当は現在 工藤さん、のちに SAWさんへ引き継ぐ予定。**置き場所は未決。**`ops.json` は3時間おきに再生成されるため直接書くと消える／公開判断をAIにさせない（運用手順 §6・§8）という制約から、「議事録シートに要約列を足して `公開` ○ のものだけ出す」形が候補。あわせて**宿題を画面から編集する案は見送り**（サーバーが無くパスワードは表示の目隠しのみ＝誰でも書ける口になる／反映が最大3時間遅れる）。**次のセッションも提案しないよう**引き継ぎ書 §7-2 に明記 | （このコミット） | 済 |
| 2026-08-12 | **保守項目は運営ログのスプレッドシートに入れない、と決めた**（運営者の判断・コード変更なし）。宿題の置き場は2つあり役割が違う（`宿題` シート＝会議で出たチームの宿題・運営タブに出る／引き継ぎ書の「いま進行中」＝ダッシュボード自体の保守・画面には出ない）。Apps Script の移管を `宿題` シートにも足すか確認し、**足さない**ことになった。あちらは会議の宿題を並べる場所で性格が変わること、`公開` を ○ にすると `ops.json` 経由で raw URL から誰でも読めることが理由。**次のセッションも提案しないよう**引き継ぎ書に明記 | （このコミット） | 済 |
| 2026-08-12 | **動画アーカイブの「企画タイプ」プルダウンを全範囲6択に揃えた**（スプレッドシートの設定変更・コード変更なし）。入力規則が **`E2:E4`（4択）/ `E5:E9`（5択）/ `E10:E292`（6択）の3つに分断**され、中身がバラバラだった。**新しい動画は2行目に追加されるため、壊れていた範囲にちょうど新作が入ってきており**、8/8・8/10・8/11 公開の3本は「国内切り抜き」を選べない状態だった。`海外切り抜き / 国内切り抜き / 大会プレー / 店舗ハンド / 解説動画 / JOPT Games` の6択に統一（`JOPT Games` と `店舗ハンド` は両方必要と運営者に確認。`店舗ハンド` は使用0件だが残す）。**入力規則は「入力を拒否」ではなく「警告を表示」**なので、選択肢に無い値も手打ちで入る（`国内切り抜き` 44件はそうやって入っていた）＝「値があるから選択肢にもある」とは読めない。引き継ぎメモ 07-31 の宿題Bはこれで完了 | （このコミット） | 済 |
| 2026-08-12 | **企画タイプのプルダウンについて、引き継ぎ書の記述が誤りだったのを訂正**（コード変更なし）。旧記述は「`E2:E6` に『国内切り抜き』が無いが `E7:E289` には有る」だったが、**実物は E2 も E7 も同じ4択**（`海外切り抜き / 大会プレー / 店舗ハンド / 解説動画`）で、どちらにも無い。にもかかわらず**値としては「国内切り抜き」44件・「JOPT Games」1件が入っている**（入力規則が「警告を表示」のため手打ちが通る）。逆に `店舗ハンド` は0件。したがって直し方は他行からのコピーではなく**入力規則そのものの編集**で、**どれを足す／消すかは運営者の判断待ち**。あわせて「入力規則が289行目まで」も訂正（実際は292行目まであり、行の増加に追いついている＝1000行に広げる判断は当面不要） | （このコミット） | 済 |
| 2026-08-12 | **「いま進行中・次にやること」を4件に整理**（運営者の指示・コード変更なし）。①クーポンシートの列追加は**担当が JOPT Games の担当者**で運営者の宿題ではないため一覧から外し、依頼内容は §10 に残した ②Apps Script のライブ通知ボット移管は **8/15（金）に運営者が作り直す**予定として手順を §10 に明記 ③旧作へのクーポン掲出は**今週のミーティングで議論中**のため一覧に残置 | （このコミット） | 済 |
| 2026-08-12 | **投稿計画表を「制限付き」のまま読めるようにした。**シートの共有が制限付きに変わったことで `fetch.py` の認証なし CSV エクスポートが **401** になり、次の自動更新で `video_archive`（企画タイプ・ナレーター別集計の元）が空になる状態だった。運営ログと同じサービスアカウントをシートに**閲覧者**として追加し、`fetch.py` に `_sheet_rows()` を新設（gid→タブ名を API で解決してから Sheets API で読む。`SHEETS_SA_KEY` が無ければ従来の CSV に落ちる）。`daily_fetch.yml` の fetch ステップに `SHEETS_SA_KEY` と条件付き `pip install google-auth` を追加。`google-auth` の依存は `fetch_ops.py` の中に閉じたままで、`fetch.py` は標準ライブラリのみを維持。**この失敗は Actions が緑のまま起きる**ため、`video_archive` が 0 件に変わった1回だけ LINE に出す検知も追加（毎回出すと月200通の枠を食うため）。**シートの共有を緩めて直さないこと**（URL は公開リポジトリに載っている） | （このコミット） | 済 |
| 2026-08-12 | **旧作へのクーポン掲出を「8/15以降に再生上位30本へ一斉」から「バズったら随時追加」に変更**（運営者の判断・コード変更なし）。上位リストは作らない。掲出日が揃わなくなるため「基準線からの増分＝旧作ぶん」という読み方は**使えなくなり**、伸びている動画を選ぶ選択バイアスも入る。よって**既定の答えは「判定できない」**。代わりに**貼るたびに動画IDと掲出日を運営ログの `施策` シートに残す**運用にした（記録が無いと後から何も言えないため）。引き継ぎ書 §10 の掲出スケジュールを書き換え | （このコミット） | 済 |
| 2026-08-12 | **引き継ぎ書が実態と食い違っていた3点を訂正**（コード変更なし）。①投稿計画表のオーナーが `ytpokerguild` に移っており、引き継ぎメモ 07-31 の「宿題A（容量超過で表示専用）」は**解消済み** ②企画タイプの空欄「約69行」は**解消**（290本中287本入力済み・未入力4本）で、「国内切り抜き」も44件使われている ③動画アーカイブの入力規則は289行までだが、**データは約291行あり既に超過している**。あわせて、Drive コネクタの `get_file_permissions` が投稿計画表では**オーナー1件しか返さない**（＝`anyone` が出ないことを「安全」と読んではいけない）ことを §9 に明記 | （このコミット） | 済 |
| 2026-08-12 | **作業手順を2つ追記（コード変更なし・引き継ぎ書 §2）。**①**グラフを直したら見た目を確認する手順を確立。**`cdnjs` がサンドボックスから届かず Chart.js が読めないため「直したつもりで直っていない」が起きうる。`npm pack chart.js` は通るので、Playwright で差し込めば実描画・スクショ・Y軸や日数の数値確認までできる ②**push の直後に `git fetch origin` を実行すること。**URL 直指定 push だと追跡refが更新されず、push 済みなのに「未 push が1件」と誤検知される（実際に発生） | （このコミット） | 済 |
| 2026-08-12 | **期間の終わりを「最後の完了日」に統一**（運営者の判断。前の行の残件を解消）。集計途中の当日をグラフからだけ外していたため、上のタイルの合計と期間A/B比較にはまだ入っており、「直近7日」が実質6.5日分＝期間Aだけ不利という状態だった。`lastCompleteDate()` を1か所に置き、期間ボタン・比較プリセット・日付欄の `max` を全部これに合わせた。**グラフ・タイル・A/B比較の3か所が同じ日で揃う**（例: 28日 = 07/15〜08/11）。全期間でも終わりだけは固定する | （このコミット） | 済 |
| 2026-08-12 | **日次グラフが数字を読み違えさせる状態だったのを修正**（運営者の指摘）。①**集計途中の当日を折れ線と7日移動平均から除外**（半日分の値が並ぶので毎回右端が急落して見えていた。実測 37,782回 対 前後 8〜9.6万回）②**面グラフ・棒グラフのY軸を0起点に強制**（Chart.js の既定は0起点ではなく、その日のデータ次第で途中から始まっていた）③**登録者数（累計）の塗りを廃止**（0起点にすると変化が潰れるため、代わりに面積を主張しない線にした）＋概数であることの注記を追加 ④スムージングを 0.4→0.15 にし点を表示 ⑤破線の意味を「確定前」だけに統一（7日移動平均は実線へ）⑥Analytics 未確定日を 0→null に（「28日分」と出ていた総再生時間が正しく25日分に）。判断待ちの残件はタイル合計と期間A/B比較（引き継ぎ書「いま進行中」#7） | （このコミット） | 済 |
| 2026-08-12 | **不具合修正: 「投稿曜日 × 平均視聴回数」「動画尺別 平均視聴回数」の2つが最初から空欄だった。**`renderDowDuration()` は `5e96069`（3タブ再編）で追加されたが `renderOverview()` から呼ばれておらず、一度も描画されていなかった。呼び出しを追加し、同じ外し方を繰り返さないよう警告コメントを添えた | （このコミット） | 済 |
| 2026-08-12 | **運営ログの共有設定が「リンクを知っている全員が編集可」になっていたのを発見し、「制限付き」に修正。**シートのURLは公開リポジトリに載っているため、実質だれでも読み書きできる状態だった。あわせてサービスアカウントを編集者→閲覧者に。**投稿計画表は同じ状態が残っている**（対処手順と順番の注意を引き継ぎ書 §9 に記載）。コード変更は無し・方針と設定の変更のみ | （このコミット） | 済 |
| 2026-08-12 | `SHEETS_SA_KEY` の貼り付け事故（BOM・前後の引用符・余分な改行・base64）を自動で吸収し、読めなかったときは**長さと先頭末尾1文字だけ**を診断として出すようにした（鍵の中身は出さない）。初回設定が「JSON として読めません」で止まったため | f906932 | 済 |
| 2026-08-12 | **運営ログの自動反映をサービスアカウント方式で構築**（`fetch_ops.py` に `SHEETS_SA_KEY` 経路を追加、`daily_fetch.yml` に環境変数と条件付き `pip install google-auth` を追加）。OAuth 方式は個人アカウントに紐づき、同意画面がテスト状態だとトークンが7日で失効するため見送り。旧方式はフォールバックとして残置。あわせて GCP プロジェクトを `ytpokerguild` と共同所有にし、スプレッドシートの所有者も移譲（引き継ぎ対策） | （このコミット） | 済 |
| 2026-08-10 | **運営ログの自動反映（§10 の保留宿題）に着手し、コード側を完了。**`fetch_ops.py` が `SHEETS_CLIENT_ID` / `SHEETS_CLIENT_SECRET` を優先して読むようにし（`fetch.py` の `OAUTH_*` はスプレッドシート権限が無いため）、`daily_fetch.yml` にも両方を渡すよう追加。**残りは Secrets 3つの登録だけ。**未設定のあいだは従来どおり `[SKIP]` で正常終了 | （このコミット） | 済 |
| 2026-08-10 | **不具合修正: 運営タブの注記に `<br>` `<b>` が生タグのまま表示されていた**（`textContent` に HTML を渡していたため）。`innerHTML` に修正し、埋め込む数値は `Number()` で固定。同じ間違いを繰り返さないようコメントを追加 | （このコミット） | 対象外 |
| 2026-08-10 | **サイト内の JSON 読み込みを全部 `{cache:'no-cache'}` に統一**（`index.html` 3か所・`report.html` 1か所）。GitHub Pages が `Cache-Control: max-age=600` を返すため「更新したのに画面が古いまま」が起きていた。「↻ 更新」は `cache:'reload'`。あわせて `video_daily.json` にも `file:` 用の URL 分岐を追加し、切り分け手順を引き継ぎ書 §11 に記載 | （このコミット） | 済 |
| 2026-08-10 | **宿題の担当者名を役割表記に置換せず、スプレッドシートの表記のまま出すよう変更**（`build_ops_json.py` の `pub_owner()`）。画面で誰の宿題か分かることを優先する運営者の判断。**`ops.json` は raw URL で誰でも読めるため担当者名は公開される**ことを説明したうえでの決定。本文中の実名は従来どおり置換。`index.html` の注記も修正 | （このコミット） | 済 |
| 2026-08-10 | **運営ログは「1行＝1タスク」に統一（運営者からの明示の指示）。**議事録の「Aの調整／Bの把握／Cの決定」のような束ねた行を分割しないと、片方が終わっても状態を正しく付けられないため。ルールを `チャンネル分析AI_運用手順.md` §8-0 に新設し、既存の宿題も分割（23行→32行）して `ops.json` を再生成 | （このコミット） | 済 |
| 2026-08-10 | **クーポンコードの割り当てと掲出スケジュールを確定**（引き継ぎ書 §10）。週次コードは入力期限なしで発行、旧作へは同じ週次コードを**1週間ずらして**貼り、先に新作だけの基準線を取る運用にした。期限つきの初回コードは延長せず短期サンプルとして切り離す | （このコミット） | 済 |
| 2026-08-10 | **クーポンの集計を「件数」と「人数」に分けた。**同じ人が別のクーポンコードを使えるようになったため、行数＝人数ではなくなった。`fetch.py` に `people` / `has_uid_column` を追加（UIDの値は出力しない）、`index.html` のクーポンタブを件数表記に直しコード別の内訳を追加。あわせて引き継ぎ書 §10 に「クーポンコードの運用」を新設（週次発行・旧作は `REPOKER01` 流用・入力期限 2026-09-01・使用期間30日・未使用枚数の読み違い注意）、古い「集計単位を土〜金」の記述を削除 | （このコミット） | 済 |
| 2026-08-10 | クーポンコードの入力期限を今回の発行分から実質撤廃。`REPOKER01`（入力期限 2026-09-01）は延長せず 8/1週の短期サンプルとして切り離し、**旧作には流用しない**方針に変更。旧作用は専用コードを別発行。**動画内に焼いたコードは差し替えられない**点とあわせて引き継ぎ書 §10 に明記 | （このコミット） | 済 |
| 2026-08-10 | **週次AI分析の起動を「チャットでの依頼のみ・定期実行なし」に統一**（運営者に確認）。引き継ぎ書 §8 と運用手順 §0 の旧記述（土曜 cron／火曜リマインダー）を両方とも撤回。あわせて `docs/` 移動時に伏せ字にした値を Dropbox の md にだけ復元し、**リポジトリ側には書かないルール**を `CLAUDE.md` に追加。廃止済みファイルの参照名も実ファイル名に修正 | （このコミット） | 済 |
| 2026-08-10 | **Dropbox の md 2つを 2026-08-10 の内容に更新し、`CLAUDE.md` の「⚠ 移行メモ」節を削除。**正本が Dropbox に戻ったことに合わせて、両 md の「正本は `docs/`」「Dropbox はミラー」「この書類は公開される」の記述を書き換え | 87f2d86 | 済 |
| 2026-08-10 | **`docs/` を廃止し、書類の正本を Dropbox に戻した**（公開される場所に書類を置かない方針）。リポジトリに残すのは `CLAUDE.md` と この表だけ | 49ac29b | 済 |
| 2026-08-10 | Dropbox「PGコンソール」の md を `docs/` のミラーとして同期し続けるルールを追加（運営者の希望）**→ 同日撤回。上の `docs/` 廃止で無効** | 4c898e1 | 済 |
| 2026-08-10 | 引き継ぎ書に「いま進行中・次にやること」の章を追加（新しいチャットの入り口） | 1a714a1 | 済 |
| 2026-08-10 | **引き継ぎ書類の正本を Dropbox から `docs/` に移動**し、公開できない値（メール・グループID・URL・実パス）を本文から外して参照先の記述に置き換え **→ 移動は同日撤回。値の伏せ字はそのまま残っている（戻すかは要判断）** | f2c7cc1 | 済 |
| 2026-08-10 | 「md の更新は担当者にやらせない」を `CLAUDE.md` に明文化（運営者からの明示の指示） | 45f3d87 | 済 |
| 2026-08-10 | 引き継ぎ書類の更新ルールを明文化。`CLAUDE.md` / `変更履歴.md` / `docs_check.py` / `docs_guard.yml` を追加し、`daily_fetch.yml` に反映漏れの通知を追加 | 43ebaa5 | 済 |
| 2026-08-10 | クーポンタブにクーポンシートへのリンクを追加（`index.html` 2か所） | 8eb3721 | 済 |
| 2026-08-10 | 週次AI分析（8/1〜8/7）を確定版に更新 | 09c52b5 | 対象外 |
| 2026-08-09 | クーポンタブを追加（`fetch.py` に `fetch_coupon()`、`index.html` に4つ目のタブ） | 0ff2a1c | 済 |
| 2026-08-09 | 運営ログの3時間自動反映（`fetch_ops.py` / `auth_sheets.py` / `daily_fetch.yml`）。トークン未設定のため休止中 | e58345f | 済 |
| 2026-08-08 | マニュアルページ `manual.html` を追加 | c78ac31 | 済 |
| 2026-08-08 | 新作初速の表示を廃止し、トップの既定期間を28日に変更 | abf5502 | 済 |
